"""OpenType table subpackage.

Mirrors ``org.apache.fontbox.ttf.table``. Currently exposes the
``common`` sub-subpackage; the ``gsub`` sub-subpackage upstream is
already covered by :mod:`pypdfbox.fontbox.ttf.gsub`.
"""
