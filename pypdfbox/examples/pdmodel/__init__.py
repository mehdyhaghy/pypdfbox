"""Examples ported from ``org.apache.pdfbox.examples.pdmodel``.

Each module here mirrors a single upstream Java class with a ``main(argv)``
entry point that accepts an optional list of command-line arguments (the
Python equivalent of Java's ``String[] args``).
"""
