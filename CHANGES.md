# CHANGES

Substantive behavioral deviations of pypdfbox vs upstream Apache PDFBox.
Per-release notes go here; trivial naming changes (camelCase → snake_case) are not listed.

## Format

```
- pypdfbox/<path>: <one-line description of deviation>
  upstream: PDFBox <version> <java path>
  reason: <why we deviate>
```

## Project-wide deviations vs upstream

- **No `preflight` module.** Apache PDFBox 4.0 removes Preflight; we follow that decision. PDF/A and PDF/UA validation is performed via external veraPDF / PAC.
- **No commons-logging / log4j.** Python `logging` (stdlib) is used throughout.
- **Method naming.** Java camelCase → Python snake_case across the entire API surface. Semantics unchanged.

## Per-file deviations

- `pypdfbox/io/random_access_read_buffer.py`: wraps `io.BytesIO` instead of reimplementing PDFBox's chunked-list storage. Observable behavior is identical; implementation is C-backed and ~25 lines instead of ~120. Justification: PRD §3.7 (stdlib-first for generic infrastructure).
- `pypdfbox/io/random_access_read_buffered_file.py`: wraps `io.BufferedReader` over a raw file fd. Stdlib provides the read-ahead buffering that upstream's `RandomAccessReadBufferedFile` implements manually. Justification: PRD §3.7.
- `pypdfbox/io/random_access_read_memory_mapped.py`: net-new optional implementation backed by `mmap.mmap`. No upstream counterpart; offered as opt-in for very large files where kernel paging beats userspace buffering. Justification: PRD §3.7 — use stdlib affordances when they fit.
- `pypdfbox/io/random_access_read_view.py`: original slice-view implementation. Mirrors the upstream `RandomAccessReadView` API; storage strategy is direct seek-on-parent rather than upstream's bounded-stream wrapper.
- `pypdfbox/io/scratch_file.py`: backed by `tempfile.SpooledTemporaryFile` (mixed mode), `tempfile.TemporaryFile` (temp-file-only), or `io.BytesIO` (memory-only). Upstream uses page-based scratch storage; we delegate spill-to-disk policy to stdlib. Behavior visible to callers (read/write/seek/clear) is identical. Justification: PRD §3.7. Default spill threshold in MIXED mode without an explicit cap is 16 MiB.

## Backfill — aligned with upstream test expectations

Driven by porting upstream JUnit tests (PRD §12.1):

- `pypdfbox/io/random_access_read.py`: added `read_fully(buf, offset, length)` (raises `EOFError` on premature EOF) and `skip(n)` (clamps to length). Mirrors upstream `RandomAccessRead.readFully` / `skip`.
- `pypdfbox/io/random_access_read_buffer.py`, `_buffered_file.py`, `_memory_mapped.py`, `_view.py`: `seek(negative)` now raises `OSError` (was `ValueError`), `seek(past_end)` clamps to length and leaves the stream at EOF (was: raised `ValueError`). Matches upstream PDFBox semantics; required by ported `seek*` tests.
- `pypdfbox/io/random_access_read_view.py`: removed `start_position + length > parent_length` validation (upstream allows logical views past parent end; reads simply stop at parent EOF). `create_view()` on a view raises `OSError` (upstream forbids).
- `pypdfbox/io/scratch_file.py::ScratchFileBuffer.create_view()` raises `NotImplementedError` (upstream's `UnsupportedOperationException`).
- `pypdfbox/cos/cos_number.py`: new abstract base `COSNumber` (parses string → `COSInteger`/`COSFloat`, handling exponential notation and Java-Long-out-of-range markers). `COSInteger`/`COSFloat` now extend `COSNumber`.
- `pypdfbox/cos/cos_float.py`: values are now clamped to **IEEE-754 single precision** (`float32`) on both `__init__` paths to match Java `float` semantics — required for upstream parity tests on `equals()`/`hashCode()`. `_normalize_negatives` recovers from PDFBOX-2990 / -3369 / -3500 / -4289 misplaced-`-` cases (`--16.33` → `-16.33`, `0.-262` → `-0.262`, `0.00000-33917698` → `-0.0000033917698`); raises `OSError` on unrecoverable double-`-`.
- `pypdfbox/cos/cos_integer.py`: added `is_valid()` / `set_valid()` for PDFBOX-5176 large-integer-out-of-Long-range marking.
- `pypdfbox/cos/cos_string.py`: `parse_hex` raises `OSError` (was `ValueError`) to mirror upstream `IOException`. Added `to_hex_string()`.
- `pypdfbox/cos/cos_stream.py`: `create_raw_input_stream()` raises `OSError` when no body. `create_input_stream(stop_filters=None)` and `create_output_stream(filters=None)` now wire up to `pypdfbox.filter.FilterFactory` and run the `/Filter` chain on read / encode it on close. Encryption-aware stream wrapping (`COSStream.create_view` over an encrypted scratch buffer) is still deferred. `/Predictor` encode + decode are both supported via `FlateDecode` / `LZWDecode` (PNG 10-15 incl. Optimum, TIFF 2).
- `pypdfbox/pdmodel/common/pd_stream.py`: `create_input_stream` and `create_output_stream` now delegate to `COSStream` (single source of truth for filter plumbing). `to_byte_array()` returns `b""` for an empty stream (upstream raises `IOException`); empty `create_input_stream()` returns an empty `BytesIO` rather than raising — `PDStream` is a typed handle that legitimately wraps fresh-and-empty streams. `get_length()` returns `int | None` (was `int`); `None` indicates no `/Length` entry *and* no body.
- `pypdfbox/cos/cos_array.py`: added `remove_object`, `remove_all`, `retain_all`, `grow_to_size`, typed `set_name`/`get_name`/`set_int`/`get_int`/`set_string`/`get_string`, `set_float_array`/`to_float_array`, `to_cos_*_list` converters, factory classmethods `of_cos_names`/`of_cos_strings`/`of_cos_integers`/`of_cos_floats`. All match upstream signatures.
- `pypdfbox/cos/cos_document.py`: added `add_xref_table`, `get_objects_by_type`, `get_linearized_dictionary` — now returns a typed `PDLinearizationDictionary` wrapper (PDF 32000-1 Annex F: `/Linearized /L /H /O /E /N /T`) by scanning the object pool for the first dictionary carrying a truthy `/Linearized` entry; result is cached. `None` when the document is not linearized.
- `pypdfbox/cos/pd_linearization_dictionary.py`: new typed wrapper around the linearization parameter dictionary. Upstream PDFBox parses the same fields inline inside `PDDocument`; pypdfbox extracts them into a dedicated class so the COS-level entry point (`COSDocument.get_linearized_dictionary`) returns a structured value rather than a raw `COSDictionary`.
- `pypdfbox/cos/cos_name.py`: added single-letter and short-name constants (`A`, `B`, `C`, `D`, `T`, `BE`, `PARAMS`, `FLATE_DECODE`, `ASCII85_DECODE`, `STANDARD_ENCODING`) referenced by ported tests.
- `pypdfbox/pdfparser/base_parser.py::read_name`: a `#` not followed by two hex digits is now kept literally rather than raising `PDFParseError`. Matches upstream `TestBaseParser.testInvalidHexSequence`.

## Filter module + Loader (parallel wave)

- `pypdfbox/filter/ascii85_decode.py`: encode/decode delegate to stdlib `base64.a85encode` / `a85decode` (which already implements PDF's base-85 + `z` shortcut + adobe `<~ ... ~>` framing). Original wrapper handles whitespace pre-cleaning, `~>` terminator stripping, and per-spec error reporting (`OSError`).
- `pypdfbox/filter/flate_decode.py`: `zlib` wrapper. PNG predictors 10-15 + TIFF predictor 2 supported on both decode and encode; predictor 15 (Optimum) picks the per-row filter type that minimises the sum of signed-absolute byte values (RFC 2083 §9.6 heuristic).
- `pypdfbox/filter/ascii_hex_decode.py`: `binascii` wrapper. Whitespace ignored, odd-digit pad with `0`, terminates on `>`.
- `pypdfbox/filter/run_length_decode.py`: ported line-for-line so encoded bytes match upstream byte-identical.
- `pypdfbox/filter/lzw_decode.py`: PDF-flavored LZW (9-12 bit codes, MSB-first, EarlyChange honored on decode; encoder always emits EarlyChange=1 with the PDFBOX-1977 final-EOD-width fix).
- `pypdfbox/filter/ccitt_fax_decode.py`: T.4 (Group 3, K=0 / K>0) and T.6 (Group 4, K<0) decode by wrapping the encoded strip in a synthetic little-endian TIFF and handing it to libtiff via Pillow. `/Columns`, `/Rows`, `/K`, `/BlackIs1`, `/EncodedByteAlign` honored; `/EndOfLine`, `/EndOfBlock`, `/DamagedRowsBeforeError` accepted but inert (libtiff handles framing). `/Rows` omitted → libtiff fills a generous over-allocated height with zero-padding past actual end-of-data; callers should supply `/Rows` from the image XObject's `/Height` for a tight buffer. Decode-only (no encoder use case yet — `encode()` raises `NotImplementedError`). Output is raw bit-packed scanlines (rows padded to whole bytes, MSB-first), the same shape PDImageXObject's decoded body expects.
- `pypdfbox/filter/jbig2_decode.py`: `/JBIG2Decode` (ISO 32000-1 §7.4.7) decode via the Rust-backed `jbig2-parser` library (MIT, newly approved dependency). `/JBIG2Globals` from `/DecodeParms` (single dict or array indexed by filter position; `/DP` short-form alias also accepted) is decoded through its own `/Filter` chain via `COSStream.to_byte_array()` and prepended to the per-image segments before handoff to `jbig2_parser.parse_jbig2`. The parser returns a PNG buffer; we re-decode it through Pillow to recover raw bilevel sample bytes. JBIG2 is bilevel by ITU-T T.88 definition, so `DecodeResult.parameters` is always populated with `/BitsPerComponent = 1` and `/ColorComponents = 1` plus the codestream's intrinsic `/Width` and `/Height`. PDF spec defines no short-name abbreviation for `/JBIG2Decode` — only the long name is registered. Decode-only (`encode()` raises `NotImplementedError`); a malformed `/JBIG2Globals` value of the wrong COS type is silently ignored rather than crashing the image decode.
- `pypdfbox/filter/_predictor.py` (new): shared PNG/TIFF predictor encode (`predict`) + decode (`unpredict`) helpers, used by both `FlateDecode` and `LZWDecode`. Replaces the previously-duplicated inline `_unpredict` blocks in each filter. Supports predictors 1, 2, 10-15 across 1/2/4/8/16-bit components and any colors/columns combo.
- `pypdfbox/cos/cos_document.py`: optional `source: RandomAccessRead | None` parameter / instance attribute so `Loader` can attach the file handle it created and have `doc.close()` close it. Caller-supplied sources stay caller-owned.
- `pypdfbox/loader.py`: top-level `Loader.load_pdf()` accepts path / `bytes` / `bytearray` / `memoryview` / `BinaryIO` / `RandomAccessRead`. Mirrors `org.apache.pdfbox.Loader.loadPDF()`. Encryption / password / `MemoryUsageSetting` parameters deferred until those features ship.
- `pypdfbox/__init__.py`: exports `Loader` so `from pypdfbox import Loader` works (matches PRD §7).
- `pypdfbox/pdfparser/pdf_stream_parser.py`: extends `COSParser`. Surfaces an `Operator` value type carrying the operator name plus optional `image_parameters` / `image_data` for inline-image (`BI` ... `ID` ... `EI`) sequences. Lenient PDFBox quirks preserved: stray `]` returns `COSNull`, isolated `+` returns `COSNull`, internal `-` in numbers dropped (PDFBOX-4064), Type-3 `d0`/`d1` operator suffix accepted.

## pdfwriter cluster #1 (full-save, traditional xref)

- `pypdfbox/pdfwriter/cos_writer.py`: cluster #1 covers full-save mode only. The following upstream paths are stubbed and raise `NotImplementedError` until later clusters land:
  - `incremental=True` constructor flag → cluster #2 (incremental save).
  - Xref-stream output (`writeXrefStream` and friends) → cluster #3.
  - Object-stream packing (`COSWriterObjectStream`) → cluster #3.
  - Encrypted documents (`is_encrypted()` → `True`) → security cluster.
  - Signatures (`SignatureInterface`, `/ByteRange` ranges) → cluster #2.
- `pypdfbox/pdfwriter/cos_writer.py`: `write(document)` accepts a `COSDocument` directly. Upstream's overloads also take `PDDocument` / `FDFDocument`; those wrappers are not yet ported (`PDDocument` → PRD §6.6 cluster).
- `pypdfbox/pdfwriter/cos_writer.py`: `/ID` synthesis uses `hashlib.sha256(time_ns + secrets.token_bytes(16))[:16]` instead of upstream's `Long.toString(idTime) + info-dict-values` digest. Same shape (two 16-byte hex strings) and same purpose; we don't have a `PDDocument.getDocumentId()` to thread in yet.

## pdfwriter cluster #2 (incremental save)

- `pypdfbox/pdfwriter/cos_writer.py`: `COSWriter(..., incremental=True, incremental_input=...)` now ships. The writer copies the source bytes to the output verbatim and appends only objects whose resolved value (or its `COSObject` wrapper) carries `is_needs_to_be_updated()=True`. The appended xref section lists only the changed/new objects, the trailer carries `/Prev = old_startxref`, `/Size = max_obj_num + 1`, and preserves `/ID` exactly as the source had it. Mirrors upstream `doWriteIncrement` + `prepareIncrement` + `doWriteXRefInc`.
- `pypdfbox/pdfwriter/cos_writer.py`: digital-signature re-signing is **not** implemented. When a source signature dict (`/Type /Sig` or `/DocTimeStamp`) contains a `/ByteRange [0 0 0 0]` placeholder (or any byterange whose third entry doesn't extend past the original file end), `save_incremental` raises `NotImplementedError` rather than silently corrupt the signature. The actual digest pipeline lands with the security cluster.
- `pypdfbox/pdfwriter/cos_writer.py`: `save_incremental` on a document with **no** dirty objects produces an output **byte-for-byte identical to the source**. Upstream's behaviour matches: nothing extra is appended. Unlike full-save mode, no `/ID` synthesis happens in incremental mode — the source's `/ID` array (if any) stays exactly as it was.
- `pypdfbox/pdfwriter/cos_writer.py`: an in-memory `BytesIO` increment buffer accumulates the appended objects + xref + trailer; the real output sink only sees source-bytes followed by buffer-contents at the very end. Mirrors upstream's `ByteArrayOutputStream` → `incrementalOutput` pipeline. The buffer is initialised with `position=0`; absolute byte offsets in the new xref are computed as `source_length + buffer_offset` at emit time (upstream cheats by seeding `position` with `inputData.length()` on the `COSStandardOutputStream`; functionally identical, easier to test in isolation).
- `pypdfbox/pdfwriter/cos_writer.py`: a CRLF separator is emitted at the very start of the increment buffer when there is at least one dirty object. This matches upstream's `getStandardOutput().writeCRLF()` at the top of `visitFromDocument` in incremental mode and guarantees an unambiguous boundary between the source's trailing `%%EOF` and the appended block. When nothing is dirty the separator is **not** emitted (preserving the byte-for-byte-identical contract).
- `pypdfbox/cos/cos_document.py`: added `get_source()` (read-only accessor for the parser-attached `RandomAccessRead`), `get_start_xref()` / `set_start_xref(offset)` (the offset of the trailing xref section, used by the incremental writer for `/Prev`).
- `pypdfbox/pdfparser/pdf_parser.py`: after parsing the trailing `startxref` value, calls `document.set_start_xref(...)` so the incremental writer can find it later.

## pdfwriter cluster #3 (xref-stream + object-stream output)

- `pypdfbox/pdfwriter/cos_writer.py`: `COSWriter(..., xref_stream=True)` (with `set_xref_stream(b)` / `is_xref_stream_output()` toggles) emits a single `/Type /XRef` indirect-object stream in place of the traditional `xref` keyword + `trailer` keyword pair (PDF 32000-1 §7.5.8). Trailer entries (`/Root`, `/Info`, `/Encrypt`, `/ID`) are folded into the xref-stream dictionary as required by §7.5.8.4. `/W` field widths are computed dynamically (`w1=1`, `w2=ceil_log256(max_offset)`, `w3=2`); `/Index` always lists the contiguous object-number ranges. The xref-stream object goes through the regular `visit_from_stream` path so its body is FlateDecode-compressed and — when an active security handler is staged — encrypted with its own per-object key. Default remains the traditional xref table.
- `pypdfbox/pdfwriter/cos_writer.py`: `COSWriter(..., object_stream=True)` (with `set_object_stream(b)` / `is_object_stream_output()` toggles) packs eligible non-stream indirect objects into one or more `/Type /ObjStm` streams (PDF 32000-1 §7.5.7), capped at 100 objects per ObjStm to mirror upstream PDFBox's `COSWriterObjectStream` ceiling. Streams, the `/Encrypt` dictionary, and signature dictionaries (`/Type /Sig`, `/Type /DocTimeStamp`) are excluded by spec. Type-2 xref entries are emitted in the surrounding xref stream; the regular indirect-object frame for a packed object is suppressed. Object-stream packing requires xref-stream output (the cluster does not auto-promote — callers must set both flags).
- `pypdfbox/pdfwriter/cos_writer.py`: hybrid xref tables (`/XRefStm` pointing back at a traditional xref) — supported by upstream for backward compatibility with PDF-1.4-only readers — are not yet emitted. Callers that need that fallback should save with `xref_stream=False`.

- `pypdfbox/contentstream/operator.py`: operands list is stored on the `Operator` instance (`get_operands()` / `set_operands()`). Upstream keeps the operand stack on `PDFStreamEngine` / `PDFStreamParser`; pypdfbox attaches operands directly to the `Operator` returned by the parser as a convenience for token consumers (the parser already does this — see `pdf_stream_parser.py`). The cached singletons returned by `Operator.get_operator(name)` for ordinary operators must therefore be treated as flyweights — do not mutate operands on a cached instance you did not just receive from the parser.
- `pypdfbox/contentstream/operator.py`: cache uses a plain `dict` plus a `threading.Lock` instead of upstream `ConcurrentHashMap.putIfAbsent`. Functionally identical under the GIL.
- `pypdfbox/contentstream/pd_content_stream.py`: `get_matrix()` is typed `Any` rather than `Matrix` because `pypdfbox.util.Matrix` is not yet ported (it lands with the rendering cluster). Subclasses may return a `COSArray` (the on-disk form) until then.

## pdmodel cluster #3 (PDStream + XObject family)

- `pypdfbox/pdmodel/common/pd_stream.py`: stream construction/output with `filters` records the `/Filter` chain but stores caller-supplied bytes as-is. Upstream writes through `COSStream.createOutputStream(filters)` and therefore encodes on write; pypdfbox defers encode-on-write until the filter encoding surface is widened. Decoding existing filtered bytes is supported through `FilterFactory`.
- `pypdfbox/pdmodel/graphics/image/pd_image_x_object.py`: image decoding, color-space wrapping, array color spaces, and rendered image conversion are deferred to rendering / graphics-color clusters. Cluster #3 exposes image metadata, raw `/ColorSpace` names, filter access, and decoded byte streams only.
- `pypdfbox/pdmodel/graphics/form/pd_form_x_object.py`: `get_matrix()` returns a plain six-float list until `pypdfbox.util.Matrix` lands with the rendering cluster.

## pdmodel cluster #5 lite (annotations)

- `pypdfbox/pdmodel/interactive/annotation/pd_annotation.py`: factory dispatch is intentionally truncated to Link/Text/Square/Circle plus `PDAnnotationUnknown`; Widget, Markup, and heavier annotation subclasses are deferred to forms/rendering clusters.
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_link.py`: action and destination accessors return raw COS objects until `PDAction` and the full `PDDestination` family land in pdmodel cluster #7.
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_square_circle.py`: inherits directly from `PDAnnotation` instead of upstream `PDAnnotationMarkup`; border style, border effect, and interior color use raw COS containers until their typed wrappers land.

## pdmodel cluster #7 partial (outlines / destinations / actions)

- `pypdfbox/pdmodel/interactive/documentnavigation/outline/*`: outline tree/list mechanics are present, with typed destination/action accessors for the common action and destination wrappers in this cluster.
- `pypdfbox/pdmodel/interactive/documentnavigation/destination/*`: ships named, Fit/FitB, FitH/FitBH, FitV/FitBV, FitR, and XYZ destinations. Page destinations resolve integer indices plus direct or indirect page objects when a `PDDocument`/`PDPageTree` context is supplied.
- `pypdfbox/pdmodel/interactive/documentnavigation/destination/pd_destination_name_tree_node.py`: lightweight destination name-tree wrapper supports flat `/Names` arrays only. Broader destination-specific adoption of generic `PDNameTreeNode` remains deferred.
- `pypdfbox/pdmodel/common/pd_name_tree_node.py` + `pd_string_name_tree_node.py`: generic `/Names`-tree base + string-keyed concrete subclass; `IOException` → `OSError`; `/Limits` placeholder slots use `COSNull.NULL` since `COSArray` slots are typed `COSBase` (Java's literal `null` has no Python equivalent); `COSArrayList` not used (`get_kids()` returns a plain `list`). `PDStringNameTreeNode.convert_value_to_cos` is additive (upstream omits the value→COS direction).
- `pypdfbox/pdmodel/common/pd_number_tree_node.py`: int-keyed analog of `PDNameTreeNode` (used by `/PageLabels`, `/ParentTree`). Mirrors PDNameTreeNode's Generic[T] + ABC pattern instead of upstream's reflection-based `convertCOSToPD`. `setKids` does not round-trip through `COSArrayList`; returns a plain `list`.
- `pypdfbox/pdmodel/pd_document_name_dictionary.py` + `pd_document_name_destination_dictionary.py` + `pd_embedded_files_name_tree_node.py` + `pd_javascript_name_tree_node.py`: typed catalog `/Names` wrapper plus its embedded-files / javascript / dests sub-wrappers. `PDJavascriptNameTreeNode` leaf type is Python `str` (the JS source body) rather than `PDActionJavaScript`. Catalog categories `/AP`, `/Pages`, `/Templates`, `/IDS`, `/URLS`, `/AlternatePresentations`, `/Renditions` are deferred (rarely used; mostly absent in upstream too).
- `pypdfbox/pdmodel/pd_document_catalog.py`: `get_names()` now returns the typed `PDDocumentNameDictionary` wrapper (was `NotImplementedError` stub).
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_widget.py`: typed widget annotation. `/AA`, `/BS`, `/MK`, `/Parent` accessors return raw COSDictionary — typed `PDFormFieldAdditionalActions`, `PDBorderStyleDictionary`, `PDAppearanceCharacteristicsDictionary`, and typed `PDField` parent are deferred (the parent typing in particular avoids a form↔annotation circular import).
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/*`: scaffold ports of `PDStructureTreeRoot`, `PDStructureElement`, `PDMarkInfo`. Upstream `PDStructureNode` base collapsed (root + element wrap COSDictionary directly until typed parent chain is needed). `getKids()` returns raw `COSBase` list (no `PDStructureElement`/`PDMarkedContentReference` typed dispatch). `getParentTree()` returns raw COS (no typed `PDNumberTreeNode<PDParentTreeValue>`). `getClassMap()` returns raw entries (no typed `PDAttributeObject`). `PDStructureElement` `/A` attributes, `/C` class names, `getStandardStructureType()`, `getPage()`/`setPage()`, the four `appendKid` overloads + `insertBefore` + `removeKid` — all deferred. `PDMarkInfo`: upstream's `setSuspect(false)`-only bug is fixed here (we write the actual value). `PDStructureTreeRoot.get_id_tree()` uses a private `PDStructureElementNameTreeNode` colocated subclass mirroring upstream's class.
- `pypdfbox/pdmodel/pd_document_catalog.py`: `get_struct_tree_root()`, `get_mark_info()`, `get_oc_properties()` now return their typed wrappers (was `NotImplementedError` stubs).
- `pypdfbox/pdmodel/interactive/form/pd_text_field.py` + `pd_button.py` + `pd_push_button.py` + `pd_radio_button.py` + `pd_check_box.py` + `pd_choice.py` + `pd_combo_box.py` + `pd_list_box.py` + `pd_signature_field.py` + `pd_variable_text.py`: typed PDField subclasses replacing `PDFieldStub` for known `/FT`. Deferred surface: `PDTextField.set_value` does not regenerate widget appearance streams nor serialize rich-text DOM into `/RV`; `PDButton.get_on_values()` returns an empty set (full impl walks each widget `/AP /N`); `PDPushButton/PDComboBox/PDListBox.constructAppearances` not ported; `PDRadioButton.get_selected_index/get_selected_export_values` not ported; `PDCheckBox.get_on_value` walks first widget kid only (falls back to `"Yes"`); `PDChoice.set_value(list)` does not validate against `/Opt` (no two-arg `set_options(export, display)` overload); `PDSignatureField` typed `PDSignature`/`PDSeedValue`/`PDSignatureLock` value wrapping deferred; `PDVariableText.get_rich_text_value` reads stream payload as best-effort UTF-8; Sejda-compat `/DA` propagation into kid widgets on `set_default_appearance` not ported.
- `pypdfbox/pdmodel/pd_document_catalog.py`: `get_acro_form()` now returns the typed `PDAcroForm` wrapper (was `NotImplementedError` stub).
- `pypdfbox/pdmodel/graphics/color/pd_output_intent.py`: lite port of `PDOutputIntent`. `/DestOutputProfile` exposed as typed `PDStream` (raw `COSStream` accessor preserved as `get_dest_output_profile_cos`). `set_data(profile_bytes, num_components=3)` embeds raw ICC bytes and writes `/N`; sniffs the ICC `acsp` magic at offset 36 (ICC.1:2010 §7.2 table 17) and warns lenient on miss. Full ICC profile parsing / `PDICCBased` typed colour-space integration still deferred.
- `pypdfbox/pdmodel/pd_document_catalog.py`: `get_output_intents()` returns `list[PDOutputIntent]` (was `NotImplementedError` stub); `add_output_intent(intent)` appends to `/OutputIntents` array.
- `pypdfbox/pdmodel/interactive/action/pd_form_field_additional_actions.py` + `pd_annotation_additional_actions.py`: typed `/AA` wrappers exposing the K/F/V/C (form field) and E/X/D/U/Fo/Bl/PO/PC/PV/PI (annotation) trigger accessors. `PDAnnotationWidget.get_actions/set_actions` and the new `PDField.get_actions/set_actions` now return/accept `PDFormFieldAdditionalActions` (was raw COSDictionary).
- `pypdfbox/pdmodel/interactive/action/pd_action_sound.py` + `pd_action_movie.py` + `pd_action_rendition.py` + `pd_action_transition.py` + `pd_action_embedded_go_to.py`: typed PDAction subtypes for /Sound, /Movie, /Rendition, /Trans, /GoToE. `PDActionSound.get_sound/set_sound` round-trips through `PDSoundStream` (new `pypdfbox/pdmodel/interactive/sound/pd_sound_stream.py`; setter still accepts raw COSBase for back-compat). `PDActionTransition.get_trans/set_trans` round-trips through `PDTransition`. `PDActionEmbeddedGoTo.get_file/set_file` round-trips through `PDFileSpecification.create_fs` and `get_d/set_d` through `PDDestination.create`. `PDActionRendition.get_annotation/set_annotation` round-trips `/AN` through `PDAnnotation.create` (Screen-subtype falls back to `PDAnnotationUnknown` until a typed `PDAnnotationScreen` lands), and `get_rendition/set_rendition` round-trips `/R` through `PDRendition.create`; raw `get_an/set_an` and `get_r/set_r` accessors retained for back-compat. Deferred typed wrappers (returned as raw COS): PDAnnotationMovie (`/Annotation`), PDTargetDirectory (`/T`).
- `pypdfbox/pdmodel/interactive/annotation/pd_border_style_dictionary.py`: typed `/BS`. `/D` (line dash pattern) returns raw `COSArray` until `PDLineDashPattern` (graphics cluster) lands. Wired into `PDAnnotationLink`, `PDAnnotationWidget`, `PDAnnotationSquareCircle` `get_border_style/set_border_style`.
- `pypdfbox/pdmodel/interactive/annotation/pd_appearance_characteristics_dictionary.py`: typed `/MK`. `/BC`/`/BG` colours return raw `COSArray` (typed `PDColor` deferred). `/I`/`/RI`/`/IX` icons return raw `COSStream` (typed `PDFormXObject` deferred). Wired into `PDAnnotationWidget.get_appearance_characteristics/set_appearance_characteristics`.
- `pypdfbox/pdmodel/interactive/annotation/pd_appearance_dictionary.py` + `pd_appearance_entry.py` + `pd_appearance_stream.py`: typed `/AP` plus dual-form `/N`/`/R`/`/D` entries (single stream OR state→stream dict). `PDAppearanceStream` does NOT yet extend `PDFormXObject` (rendering cluster). Added `PDAnnotation.get_appearance_dictionary/set_appearance_dictionary` for `/AP`.
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_line.py` + `pd_annotation_free_text.py`: Line + FreeText annotations. Deferred per-subtype: `/LLO`, callout-line `/CL` geometry, FreeText `/LE`, BS/BE styling, measurement intents.
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_file_attachment.py` + `pd_annotation_rubber_stamp.py` + `pd_annotation_popup.py`: FileAttachment (`/FS` typed via `PDFileSpecification.create_fs`), RubberStamp/Stamp, Popup. Popup `/Parent` returns raw COSBase (typed PDAnnotationMarkup parent deferred to avoid circular imports).
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_markup.py` + `pd_annotation_text_markup.py` + 8 concrete markup subtypes (`pd_annotation_highlight.py`, `pd_annotation_underline.py`, `pd_annotation_strikeout.py`, `pd_annotation_squiggly.py`, `pd_annotation_caret.py`, `pd_annotation_ink.py`, `pd_annotation_polygon.py`, `pd_annotation_polyline.py`): markup base + typed text-markup intermediate exposing `/QuadPoints`. `PDAnnotationMarkup.get_in_reply_to()` returns raw COSBase (avoids recursive factory dispatch). Per-subtype deferred: `/Popup`, `/RC` rich contents, `/IC` interior color, `/BS`, measure dictionary, custom appearance handlers.
- `pypdfbox/pdmodel/interactive/action/pd_document_catalog_additional_actions.py`: catalog `/AA` typed wrapper exposing WC/WS/DS/WP/DP triggers. Wired into `PDDocumentCatalog.get_actions/set_actions`.
- `pypdfbox/pdmodel/common/pd_metadata.py`: typed catalog `/Metadata` wrapper extending `PDStream`. Multi-arg `__init__` dispatches None/COSStream/bytes/str/PDDocument-like. `get_metadata_as_string()` is the bridge to xmpbox; no `XMPMetadata`-returning accessor yet. `PDDocumentCatalog.get_metadata/set_metadata` wired (was the last `NotImplementedError` stub).
- `pypdfbox/pdmodel/graphics/color/pd_color.py` + `pd_color_space.py` + `pd_device_color_space.py` + `pd_device_gray.py` + `pd_device_rgb.py` + `pd_device_cmyk.py`: foundational color model. Each device color space exposes `INSTANCE` singleton. Deferred: `PDColorSpace.create()` factory, `to_rgb`/`to_rgb_image` rendering conversion, `PDDeviceN`, `PDPattern`, `PDIndexed`, `PDSeparation`, `PDICCBased`, `PDLab`, `PDCalGray`, `PDCalRGB`, default-color-space lookup.
- `pypdfbox/pdmodel/interactive/action/pd_target_directory.py`: typed `/T` target dictionary for `PDActionEmbeddedGoTo`. Per PDF 32000-1 Table 202: `get_target_filename/set_target_filename` access `/N` (embedded-file name in target catalog `/EmbeddedFiles`); `get_page_number/set_page_number` access `/P` integer page-index form; `get_named_destination/set_named_destination` access `/P` named-destination string form (`/P` may legally carry either type).
- `pypdfbox/pdmodel/font/*`: scaffold port of `PDFont` (abstract) + `PDSimpleFont` (intermediate) + `PDType1Font` + `PDTrueTypeFont` + `PDType0Font` + `PDFontDescriptor` + `PDFontFactory`. Lite — actual font program parsing (PFB/TTF/CFF/OpenType), encoding lookup, glyph metrics, character mapping, `PDCIDFont` family, `PDType3Font`/`PDMMType1Font`/`PDType1CFont`, `getFontMatrix`/`getDisplacement`/`isVertical`, Standard 14 AFM, and font subsetting are deferred.
- `pypdfbox/pdmodel/graphics/pd_line_dash_pattern.py`: typed `/D` dash pattern. Wired into `PDBorderStyleDictionary.get/set_dash_style` and `PDExtendedGraphicsState.get/set_line_dash_pattern` (both setters accept either typed wrapper or raw `COSArray` for backwards compat). Phase accepts `float` (upstream uses `int`).
- `pypdfbox/pdmodel/graphics/state/pd_extended_graphics_state.py`: typed `/ExtGState` wrapper exposing /LW /LC /LJ /ML /D /RI /CA /ca /BM /SMask(raw) /AIS /TK /SA /OP /op /OPM /SM /FL accessors. `/Font` exposes only `get_font_size` helper (full `PDFontSetting` deferred). `/SMask`/`/TR`/`/TR2`/`copy_into_graphics_state` deferred. `/BM` blend mode raw `COSName | str` (typed `BlendMode` enum deferred).
- `pypdfbox/pdmodel/interactive/form/pd_xfa_resource.py`: typed `/XFA` wrapper. `is_dynamic()` (upstream has no such method) inspects the parsed XFA payload via stdlib `xml.etree.ElementTree`, locates the `<template>` packet and its first `<subform>` child (namespaced or not), and returns `True` when the `layout` attribute is set and not `"position"`; falls back to a substring heuristic (`<xfa:datasets`, `<xdp:xdp`, `subform name="form1"`) on malformed XML or missing template/subform. Result is cached on the instance. `get_document()` returns a stdlib `xml.etree.ElementTree.Element` (root) instead of upstream's W3C `org.w3c.dom.Document`; result is cached on the instance and parsing is lazy. Added `get_document_as_xml()` returning the UTF-8-decoded payload. `PDAcroForm.xfa()` returns the typed wrapper.
- `pypdfbox/pdmodel/interactive/digitalsignature/*`: lite ports of `PDSignature`, `PDSeedValue`, `PDSignatureLock`. `PDSignatureField.get_signature/set_value/get_seed_value/set_seed_value/get_lock/set_lock` now return/accept typed wrappers (or raw `COSDictionary` for backwards compat). Actual signing/PKCS#7/timestamp generation deferred.
- `pypdfbox/pdmodel/graphics/pd_property_list.py` + `pd_optional_content_membership_dictionary.py`: `PDPropertyList` abstract base + `PDOCMD`. `PDOptionalContentGroup` now extends `PDPropertyList` (was: didn't, called out in earlier wave). `PDPropertyList.create()` returns `None` for unknown `/Type` (upstream returns a bare `PDPropertyList`). `/VE` visibility-expression tree parsing deferred per upstream.
- `pypdfbox/pdmodel/graphics/color/pd_pattern.py` + `pd_indexed.py` + `pd_separation.py` + `pd_device_n.py` + `pd_icc_based.py` + `pd_cal_gray.py` + `pd_cal_rgb.py` + `pd_lab.py`: 8 new color spaces extending `PDColorSpace`. Plus `PDColorSpace.create()` static factory dispatching on `COSName` / `COSArray`. ICC profile parsing/embedding, `to_rgb` conversion, pattern/tint-transform evaluation, gamut mapping deferred. `PDIndexed` lookup table now consumes the `/Filter` chain via `COSStream.create_input_stream` when `/Lookup` is a stream, returning decoded palette bytes; raw `COSString` lookups pass through unchanged. Output length is clamped to `(hival + 1) * base_components` (truncate-or-zero-pad) per upstream's lenient handling.
- `pypdfbox/pdmodel/graphics/color/pd_icc_based.py` + `pd_separation.py` + `pd_device_n.py` + `pd_pattern.py` + `pd_color.py`: `to_rgb(components)` added on each color space; `PDColor.to_rgb` now delegates for ICCBased/Separation/DeviceN/Pattern. `PDICCBased` falls back to `/Alternate` (or one inferred from `/N` ∈ {1,3,4} → DeviceGray/DeviceRGB/DeviceCMYK) — embedded ICC profile parsing remains deferred (library-first per CLAUDE.md). `PDSeparation` and `PDDeviceN` evaluate the tint transform via `PDFunction.create(...).eval(...)` and forward to the alternate. `PDPattern.to_rgb` recurses into the underlying CS for uncolored tiling; colored patterns (no underlying) return `None` so `PDColor.to_rgb` raises `NotImplementedError` — full pattern/shading rendering belongs to the rendering module. `PDDeviceN` gains `get/set_tint_transform` accessors mirroring `PDSeparation` (upstream has both).
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_structure_node.py` + `pd_attribute_object.py` + `revisions.py`: struct base + attribute object + Revisions[T] helper. `PDStructureElement` gains `/A` and `/C` typed accessors. Typed `PDAttributeObject` owner subclasses (`PDLayoutAttributeObject` etc.) deferred. `PDStructureTreeRoot` and `PDStructureElement` extend `PDStructureNode` — `/K` kid mechanics (`get_kids`/`set_kids`/`append_kid`/`remove_kid`) live on the base; subclasses keep `_root`/`_element` as backwards-compat aliases for the inherited `_dictionary`.
- `pypdfbox/pdmodel/interactive/documentnavigation/destination/pd_page_destination.py`: added `find_page_number()` + `retrieve_page_number()` returning the integer index when `/D[0]` is `COSInteger`; direct page dictionaries and indirect page objects now resolve by COS dictionary identity when a `PDDocument` or `PDPageTree` context is supplied. `get_page()` return type narrowed from `COSBase | None` to `COSDictionary | None`.
- `pypdfbox/pdmodel/interactive/action/pd_action_remote_go_to.py`: added `get_named_destination` / `set_named_destination` for the string-form `/D` named-destination case.
- `pypdfbox/pdmodel/common/filespecification/*`: lite ports of `PDFileSpecification`, `PDComplexFileSpecification`, `PDSimpleFileSpecification`, `PDEmbeddedFile`. `PDEmbeddedFile.get/set_creation_date/mod_date` use raw `COSString` PDF date strings (no `datetime` conversion until a cos date helper lands). `PDEmbeddedFile.set_check_sum` stores via `COSString` (no `setEmbeddedString` helper yet). `PDEmbeddedFile` constructor variants `(PDDocument, InputStream[, COSName filter])` collapsed into a single dispatching `__init__` mirroring our `PDStream` constructor pattern. Small private `_get/_set_embedded_int/_string` helpers live in `pd_embedded_file.py` because `COSDictionary` does not yet expose `getEmbeddedInt`/`getEmbeddedString` — promote later.
- `pypdfbox/pdmodel/graphics/optionalcontent/*`: lite ports of `PDOptionalContentGroup` + `PDOptionalContentProperties`. `get_render_state(destination)` accepts a string ("Print"/"View"/"Export") since upstream `RenderDestination` enum lives in `org.apache.pdfbox.rendering` (not yet ported). `BaseState`/`RenderState` enums collapsed to plain strings. `PDOptionalContentGroup` does not extend `PDPropertyList` (parent class not yet ported). `PDOptionalContentProperties.add_group` defensively sets `/Type /OCG` on the supplied dict (slight superset of upstream). `compute_visible_ocgs(destination=None)` resolves /D BaseState + /ON + /OFF to a set of `id(ocg)` ready for `PDOptionalContentMembershipDictionary.is_visible`; when `destination` ("View"/"Print"/"Export") is supplied, the /D /AS auto-state usage array (PDF 32000-1 §8.11.4.4 Table 102) is now applied as a final pass — for each /AS entry whose /Event matches the destination, each OCG's /Usage/<category>/<Event>State is consulted in spec /Category order (first non-"Unchanged" wins) and the visible set is updated ("ON" adds, "OFF" discards, "Unchanged" leaves prior decision). "Unchanged" /D /BaseState is still treated as "ON" baseline at first call (no per-session state preserved).
- `pypdfbox/pdmodel/interactive/pagenavigation/*`: lite port of `PDTransition` plus `PDTransitionStyle`/`Motion`/`Dimension`/`Direction` as plain classes with constants instead of `enum.Enum`, mirroring `PDActionXxx.SUB_TYPE` pattern. `get_direction()` returns `int` with `-1` sentinel for `/None` rather than the raw `COSBase`. Upstream `setFlyAreaOpaque(boolean)` is exposed as `set_fly_area_to_show(b)` (key `/B` unchanged) per local naming.
- `pypdfbox/pdmodel/interactive/form/*`: scaffold port of `PDAcroForm`, `PDField`, `PDNonTerminalField`, `PDTerminalField`, `PDFieldFactory`, plus a generic `PDFieldStub` returned for all terminal fields until typed subclasses land. **Deferred upstream surface:** `PDAcroForm.flatten`, `refresh_appearances`, `import_fdf`/`export_fdf`, `get/set_default_resources`, `get/set_default_appearance`, `get/set_q`, `get/set_calc_order`, `get/set_scripting_handler`, `get_field_iterator`/`get_field_tree` (`PDFieldTree` not ported — recursive walk inlined in `get_field`), `set_cache_fields`/`is_caching_fields`, typed `PDXFAResource` (`xfa()` returns raw COS); `PDField.get/set_value`/`get_value_as_string`, typed `PDFormFieldAdditionalActions` (`/AA`), per-field `export_fdf`; `PDNonTerminalField.get_value/get_default_value` inheritance helpers; `PDTerminalField.set_actions/set_widgets/apply_change/construct_appearances/import_fdf/export_fdf` plus `PDAnnotationWidget` typing (`get_widgets()` returns raw `COSDictionary` list); `PDFieldFactory` per-`/FT` dispatch (Tx/Btn/Ch/Sig) — all terminal fields currently return `PDFieldStub`.
- `pypdfbox/pdmodel/interactive/action/*`: ships base/factory plus GoTo, URI, Named, Launch, RemoteGoTo, JavaScript, SubmitForm, ResetForm, ImportData, Hide, Thread, and Unknown wrappers. SubmitForm/ResetForm/ImportData/Hide/Thread expose raw COS entries where upstream depends on deferred wrappers such as file specifications, destinations, or article beads. Richer actions (Sound, Movie, Rendition, Trans, GoToE) are deferred and currently preserve as `PDActionUnknown`.
- `pypdfbox/pdmodel/interactive/action/pd_page_additional_actions.py`: ships page-open `/O` and page-close `/C` triggers. Other additional-actions dictionaries for annotations/forms are deferred to their owning clusters.
- `pypdfbox/pdmodel/pd_document_catalog.py`: `get_dests()` returns the lightweight `PDDestinationNameTreeNode` wrapper for `/Dests` dictionaries.

## Wave 7 — fonts, encoding, patterns, shading, taggedpdf, content stream writer

- `pypdfbox/pdmodel/font/standard14_fonts.py`: 14 base fonts + ~30 aliases. `get_average_widths` returns 256-element flat table (600 monospace, 500 others). Per-glyph AFM widths + kerning deferred to AFM-bundling cluster. **(Superseded — see "Standard 14 AFM bundling" below.)**
- `pypdfbox/pdmodel/font/encoding/*`: Encoding (abstract) + DictionaryEncoding + 6 predefined singletons. 256-entry tables imported from existing `pypdfbox.fontbox.encoding._TABLE` constants (byte-identical to upstream).
- `pypdfbox/pdmodel/graphics/pattern/*`: PDAbstractPattern + PDTilingPattern (type 1) + PDShadingPattern (type 2). PDAbstractPattern.create() factory dispatches on /PatternType.
- `pypdfbox/pdmodel/graphics/shading/*`: PDShading abstract base + 7 shading types. Mesh-data decoding for types 4-7, function evaluation, AWT bridges deferred (rendering cluster).
- `pypdfbox/pdmodel/documentinterchange/taggedpdf/*`: 7 typed PDAttributeObject subclasses (PDStandardAttributeObject base + Layout/List/PrintField/Table/ExportFormat/User). PDAttributeObject.create() factory wired to dispatch on /O. PDUserAttributeObject exposes /P entries as plain dicts (`{N,V,F,H}`) instead of upstream's PDUserProperty wrapper.
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_structure_tree_root.py` + `pd_structure_element.py`: now properly extend PDStructureNode (deferred from wave 6). PDStructureTreeRoot.get_kids() returns `[]` instead of `None` when /K absent (matches upstream contract).
- `pypdfbox/pdmodel/interactive/action/pd_target_directory.py`: spec deviation fixed. `/N` accessors renamed to `get_target_filename/set_target_filename`. New `get_named_destination/set_named_destination` accesses `/P` string form (per Table 202).
- `pypdfbox/pdmodel/pd_page_content_stream.py`: high-level user-facing drawing API. Path/text/color/graphics-state/XObject operators. Auto-allocates resource keys (F0/Im0/Form0). Supports AppendMode-style overwrite/append/prepend and optional FlateDecode compression on close. Deferred: actual font.encode (uses Latin-1/UTF-16BE fallback), bytes-level show_text, strict graphics-state stacking, BMC/BDC/EMC.
- `pypdfbox/cos/cos_stream.py`: replaced filter NotImplementedError stubs. `create_input_stream(stop_filters=None)` walks /Filter chain through FilterFactory. `create_output_stream(filters=None)` returns encoding stream for chained filters. `to_byte_array()` / `to_raw_byte_array()` / `set_data(data, filters)`. Encryption-aware streams deferred. Encode-side predictor now wired through `pypdfbox.filter._predictor` (PNG 10-15, TIFF 2).
- `pypdfbox/pdmodel/common/pd_stream.py`: refactored to delegate to COSStream. Added get/set_decode_parms, get/set_metadata, broadened get_length to int|None.
- `pypdfbox/pdmodel/pd_document_information.py`: get_metadata_keys() now returns sorted `list[str]` (was `set[str]`).
- `pypdfbox/pdmodel/pd_page_labels.py`: STYLE_* constants, get_label_for_page(index), get_label_ranges(), set_label_range, to_roman/to_letters helpers.
- `pypdfbox/contentstream/operator/*`: lite operator dispatch scaffold — OperatorProcessor abstract base + OperatorRegistry + 13 base no-op handlers (Tj/TJ/'/"/Tf/Td/TD/Tm/q/Q/m/l). Real semantics live in engine-coupled handlers (rendering / text-extraction clusters).

## fontbox CMap cluster

- `pypdfbox/fontbox/cmap/cmap_parser.py`: predefined CMap loading currently supports only programmatic `Identity-H` and `Identity-V`; file-backed predefined CMaps such as `Adobe-Japan1-UCS2` are deferred.

## fontbox TrueType font program + PDTrueTypeFont width lookup

- `pypdfbox/fontbox/ttf/true_type_font.py`: SFNT/TTF/OTF parsing is delegated to the (MIT-licensed) `fontTools.ttLib` library instead of the original hand-rolled directory walker, per the project's library-first rule for generic font/format concerns. The PDFBox-faithful accessor surface (`get_units_per_em`, `get_number_of_glyphs`, `get_advance_width`, `get_unicode_cmap_subtable`, `get_header` / `get_horizontal_header` / `get_maximum_profile` / `get_horizontal_metrics`, `get_table_map`) is preserved unchanged, and the existing typed-table classes (`HeaderTable`, `HorizontalHeaderTable`, `HorizontalMetricsTable`, `MaximumProfileTable`) are still returned, populated from fontTools' parse output. fontTools is imported lazily on first TTF construction so callers that never touch a TTF stream don't pay its import cost. Convenience attribute-style accessors `unitsPerEm` / `numGlyphs` / `advance_widths` were added to expose the same data without a getter call. Glyph outlines (`glyf`/`loca`), GSUB / GPOS, kerning, name-table accessors, and the full `TTFParser` / `TrueTypeCollection` surface are still deferred.
- `pypdfbox/pdmodel/font/pd_true_type_font.py`: `get_glyph_width(code) -> float` now resolves widths in PDF spec §9.7.3 order — `/Widths` (within `[FirstChar, LastChar]`) wins, otherwise the embedded `/FontFile2` is parsed and its `hmtx` advance is scaled by `1000 / unitsPerEm`. Returns `0.0` when neither source is available. CIDToGIDMap and richer Type1-style fallbacks are deferred.

## tools cluster #1

- `pypdfbox/tools/merge.py`: cluster #1 performs naive page-list concatenation and does not remap cross-document references such as links, named destinations, structure-tree owners, or form resources. This is replaced by `PDFMergerUtility` semantics when the multipdf cluster lands.
- `pypdfbox/tools/split.py`: cluster #1 copies page COS dictionaries into fresh documents without remapping references to sibling pages or document-level structures.
- `pypdfbox/tools/decrypt.py`: cluster #1 is pass-through for unencrypted PDFs and returns non-zero for encrypted inputs; real security removal depends on pdmodel cluster #10.
- `pypdfbox/tools/version.py`: prints Python/runtime package metadata in addition to the upstream-style version string.

## pdmodel viewer preferences enrichment

- `pypdfbox/pdmodel/pd_viewer_preferences.py`: adds accessors not present in upstream PDFBox 3.0 — `pick_tray_by_pdf_size`/`set_pick_tray_by_pdf_size`, `get_num_copies`/`set_num_copies` (default 1), `get_print_page_range`/`set_print_page_range` (raw `COSArray` of int pairs), `get_enforce`/`set_enforce` (PDF 2.0). Covers PDF 32000-1 / 32000-2 Table 150 entries upstream omits.

## Wave 8 — fonts (CID, Type3/MMType1/Type1C), name trees, function family, MCR/OBJR, operators, enrichment

- `pypdfbox/pdmodel/font/pd_cid_font.py` + `pd_cid_font_type0.py` + `pd_cid_font_type2.py` + `pd_cid_system_info.py`: PDCIDFont abstract + Type0/Type2 + system info. Wired into `PDType0Font.get_descendant_font()` (was raw COSDictionary). CID width lookup via `/W` + `/DW` and Type2 `/CIDToGIDMap` byte interpretation are present; full TrueType font-program metrics are deferred.
- `pypdfbox/pdmodel/font/pd_cid_font.py`: parses `/W` and `/W2` per PDF 32000-1 §9.7.4.3 — `get_widths()` (CID → width map), `get_glyph_width(cid)` (with `/DW` fallback), `get_default_width()`, `get_widths2()` (CID → (w1y, v_x, v_y) triples), `get_default_position_vector()` (defaults to spec `(880, -1000)`). Mirrors PDFBox `PDCIDFont.readWidths()` / `readVerticalDisplacements()` — same lenient interleaved-form parsing, same lazy caching.
- `pypdfbox/pdmodel/font/pd_type3_font.py` + `pd_mm_type1_font.py` + `pd_type1c_font.py`: remaining font subtypes. PDFontFactory now dispatches Type3 + MMType1. Type1C still routes to PDType1Font from factory because /Subtype is /Type1 — auto-dispatch from /FontDescriptor /FontFile3 /Subtype /Type1C deferred.
- `pypdfbox/pdmodel/font/pd_font_factory.py`: dispatch now inspects `/FontDescriptor /FontFile3 /Subtype` so `/Type1` with a Type1C-marked FontFile3 routes to `PDType1CFont`, and `/CIDFontType0` with a `CIDFontType0C`-marked FontFile3 routes to `PDCIDFontType0` (previously the factory returned `None` and Type1C was reachable only via direct construction). Plain `/Type1` (no FontFile3, or FontFile3 of `/Subtype /OpenType` etc.) still returns `PDType1Font`; bare `/CIDFontType0` without the CFF marker still returns `None` (reached via the Type0 descendant path). Closes the deferred dispatch noted in the "Type 1 / CFF font program parsing" section below.
- `pypdfbox/pdmodel/common/function/*`: PDFunction abstract + Type0/Type2/Type3/Type4. PDFunction.create() factory dispatches on /FunctionType. Numeric `eval()`, Type 4 PostScript instruction parsing, Type 0 sampled-table decoding deferred.
- `pypdfbox/pdmodel/pd_pages_name_tree_node.py` + `pd_templates_name_tree_node.py` + `pd_ids_name_tree_node.py` + `pd_urls_name_tree_node.py` + `pd_alternate_presentations_name_tree_node.py` + `pd_renditions_name_tree_node.py`: 6 typed catalog `/Names` sub-trees. Wired into PDDocumentNameDictionary (was deferred). PDIDSNameTreeNode leaf type is `bytes` (deviates from upstream `String`).
- `pypdfbox/pdmodel/interactive/documentnavigation/outline/pd_outline_item.py`: enrichment — FLAG_ITALIC/FLAG_BOLD constants, get/set_text_color (RGB tuple or None), get/set_text_flags + is_italic/is_bold, get/set_structure_element (raw COSDictionary; typed PDStructureElement deferred to avoid circular import), get/set_count + is_collapsed, find_destination_page(document) resolver.
- `pypdfbox/pdmodel/pd_viewer_preferences.py`: enrichment — pick_tray_by_pdf_size, get/set_num_copies, get/set_print_page_range, get/set_enforce. (Already documented in earlier section.)
- `pypdfbox/contentstream/operator/*`: 41 more no-op operator stubs (path c/v/y/h/re/S/s/f/f*/F/B/b/B*/b*/n/W/W*, text-state Tr/Ts/Tc/Tw/Tz/TL/T*, color CS/cs/SC/SCN/sc/scn/G/g/RG/rg/K/k, marked-content BMC/BDC/EMC/MP/DP). Default registry now exposes 53 operators (12 prior + 41 new).
- `pypdfbox/pdmodel/common/filespecification/pd_embedded_file.py`: enrichment — datetime conversion for /CreationDate /ModDate (was raw COSString), checksum bytes (with hex-form encoding), size as int|None (was -1 sentinel), creator accessor, mac_subtype/mac_creator, mac_resource_fork as COSStream (was string).
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_marked_content_reference.py` + `pd_object_reference.py`: PDMarkedContentReference (/MCR) + PDObjectReference (/OBJR) typed kid wrappers. PDStructureNode.wrap_kid() static helper auto-dispatches /K entries based on /Type (StructElem → PDStructureElement, MCR → PDMarkedContentReference, OBJR → PDObjectReference, COSInteger → MCID int).
- `pypdfbox/pdmodel/pd_page_label_range.py`: enrichment — start_index field, set_style validates against STYLE_* constants, compute_label_for_offset(offset) renders label using existing to_roman/to_letters helpers. PDPageLabels.get_label_ranges() now returns `list[PDPageLabelRange]` (was list of dicts).

## Encryption integration wiring (cluster #10)

- `pypdfbox/cos/cos_stream.py`: added `set_security_handler(handler, obj_num, gen_num)` and an encryption-aware decode path in `create_input_stream`. When a handler is attached, raw bytes are decrypted in-place before the `/Filter` chain runs; a `_decrypted` flag prevents double-undo. Mirrors the upstream `COSStream` decryption hook.
- `pypdfbox/pdfparser/pdf_parser.py`: added `get_encryption_dictionary()` and `get_document_id()` accessors for the trailer-level `/Encrypt` and `/ID[0]` entries — needed by `PDDocument.decrypt` for file-encryption-key derivation.
- `pypdfbox/pdfparser/pdf_parser.py`: added `set_password()`, `get_password()`, `get_security_handler()`, `has_encrypted_xref_streams()` for the early-decryption hook needed by encrypted xref-stream PDFs (PDF 1.5+). When a password is staged the parser instantiates a `StandardSecurityHandler` the moment the trailer + `/Encrypt` + `/ID` are visible — *before* xref-stream entries are decoded — so the cluster-#4 xref-stream decoder (when it lands) can decipher the body. The legacy lazy flow (no password staged → `PDDocument.decrypt` walks the pool retroactively) still works unchanged. Xref-stream *entry* decoding itself remains deferred to parser cluster #4; encrypted xref-stream parsing currently sets `has_encrypted_xref_streams() = True` and stages the handler before falling through to the cluster-#4 placeholder.
- `pypdfbox/pdmodel/pd_document.py`: `is_encrypted` (already present) joined by `decrypt(password)`, `protect(StandardProtectionPolicy)`, `get_encryption() → PDEncryption | None`, `set_encryption_dictionary`, `get_current_access_permission`. `decrypt` instantiates `StandardSecurityHandler`, runs `prepare_for_decryption`, then attaches the handler to every `COSStream` in the object pool. `protect` only accepts `StandardProtectionPolicy`; public-key handler dispatch raises `NotImplementedError` (deferred to the public-key cluster). `save()` honours `set_all_security_to_be_removed(True)` by forcing a decode pass on every loaded stream and stripping `/Encrypt` from the trailer before delegating to the writer.
- `pypdfbox/loader.py`: `Loader.load_pdf` accepts an optional `password` positional argument. When the parsed document carries `/Encrypt` and a password is supplied (empty string counts), a transient `PDDocument` wrapper invokes `decrypt(password)` before the `COSDocument` is returned. `PDDocument.load` mirrors the password parameter. Encrypted documents loaded without a password are returned encrypted — callers can wrap and call `decrypt` manually.

## Wave 9 — encryption cluster + typed wrappers + PDFunction.eval

- `pypdfbox/pdmodel/encryption/pd_encryption.py` + `access_permission.py` + `protection_policy.py` + `standard_protection_policy.py`: PDEncryption (/Encrypt wrapper) + AccessPermission (permission flag wrapper, NOT COSDictionary-backed; wraps an int) + ProtectionPolicy (abstract) + StandardProtectionPolicy. AccessPermission constants exposed as 1-based bit *positions* for readability while the wrapped int stores conventional masks.
- `pypdfbox/pdmodel/encryption/security_handler.py` + `standard_security_handler.py` + `security_provider.py`: SecurityHandler abstract + StandardSecurityHandler covering revisions 2-6 (RC4-40, RC4-128, AES-128 CBC, AES-256, AES-256+SHA-384+PBKDF2) using the `cryptography` library. `is_aes()` keys off `/StmF` name (`AESV2` / `AESV3`); `/CF` crypt-filter sub-dictionary parsing deferred. Per-revision key-length validation (r2 must be 40, r3-r4 must be 40-128) deferred — trusts dictionary. PDInvalidPasswordException raised on no-match. Used `cryptography.hazmat.decrepit.ciphers.algorithms.ARC4` to silence deprecation.
- `pypdfbox/pdmodel/encryption/public_key_security_handler.py` + 3 companions: PublicKeySecurityHandler decrypt path using `cryptography.hazmat.primitives.serialization.pkcs7.pkcs7_decrypt_der`. `prepare_document` (encrypt path) raises NotImplementedError — full CMS envelope construction deferred.
- `pypdfbox/cos/cos_stream.py`: encryption-aware `create_input_stream` decrypts raw bytes via attached SecurityHandler before running the /Filter chain. New `set_security_handler(handler, obj_num, gen_num)` setter.
- `pypdfbox/pdfparser/pdf_parser.py`: added `get_encryption_dictionary()` and `get_document_id()` accessors over the trailer.
- `pypdfbox/pdmodel/pd_document.py`: `is_encrypted()`, `decrypt(password)`, `protect(StandardProtectionPolicy)`, `get_current_access_permission()`, `set_all_security_to_be_removed(b)`. `save()` honors strip-on-save by decoding streams + dropping /Encrypt.
- `pypdfbox/loader.py`: `Loader.load_pdf(source, password=None)` — auto-decrypts when document is encrypted and password is supplied.
- `pypdfbox/pdmodel/interactive/digitalsignature/pd_signature.py`: `verify(document_bytes) -> SignatureValidationResult` extracts signer cert via `cryptography.pkcs7.load_der_pkcs7_certificates`. **Returns `is_valid=False` with a deferred-feature error** because cryptography's `pkcs7` module exposes only signing builders; full SignedData chain validation needs `asn1crypto` (deferred).
- `pypdfbox/pdmodel/interactive/annotation/pd_ink_list.py` + `pd_path_info.py` + `pd_line_info.py` + `pd_vertices.py`: typed wrappers for /InkList, /Vertices, /L geometry. PDAnnotationInk.get_ink_list now returns PDInkList (was raw COSArray); polygon/polyline get_vertices kept returning `list[float]` for backwards compat — typed PDVertices is exported separately.
- `pypdfbox/pdmodel/common/function/`: PDFunction.eval(input) abstract + PDFunctionType2.eval (exponential interpolation per §7.10.3) + PDFunctionType3.eval (stitching per §7.10.4). Plus clip_input / clip_output / get_ranges_for_inputs / get_ranges_for_outputs helpers. Type 0 sampled-table eval and Type 4 PostScript eval deferred.
- `pypdfbox/contentstream/operator/{imagecontent,graphics}/*` + `state/{set_dash_pattern,set_flatness,set_rendering_intent,set_graphics_state_parameters}.py`: 9 more no-op operator handler stubs (BI, ID, EI, Do, cm, d, i, ri, gs). Default registry now exposes 62 handlers.
- `pypdfbox/pdmodel/documentinterchange/taggedpdf/pd_four_colours.py` + helpers in pd_standard_attribute_object.py: PDFourColours typed wrapper (4-side color array for /BorderColor etc.). PDLayoutAttributeObject get_background_color/get_color now return `tuple[float, ...]` (was raw COSArray); new get_border_color returns PDFourColours.
- `pypdfbox/pdmodel/interactive/action/pd_action_embedded_go_to.py`: `walk_to_target() -> list[TargetStep]` walks the chained /T target dictionaries. TargetStep dataclass exposes (relationship, target_filename, page_number, named_destination, annotation_number). Cycle detection deferred.
- `pypdfbox/pdmodel/interactive/action/pd_action_embedded_go_to.py`: `resolve_target(source_document, target_document=None) -> tuple[PDDocument, PDPageDestination | PDPage] | None` walks the /T chain through /Names /EmbeddedFiles, opens each embedded file as a fresh PDDocument, and resolves the action's /D against the final scope's /Names /Dests name tree. /R="P" steps pop to the supplied target_document; missing /N or unreadable embedded bytes return None gracefully (no raise).
- `pypdfbox/pdmodel/interactive/action/pd_action_remote_go_to.py`: `get_destination()` typed dispatch — returns PDDestination for COSArray /D, str for COSString/COSName /D, None when absent.

## /CF crypt-filter dispatch

- `pypdfbox/pdmodel/encryption/pd_crypt_filter_dictionary.py` (NEW): typed `PDCryptFilterDictionary` wrapper. Fresh instances are tagged `/Type /CryptFilter`; wrapping a parsed dictionary leaves entries untouched. `/Length` reads in **bytes** (default 5 = 40-bit RC4) per §7.6.5 Table 25 — note this differs from `PDEncryption.get_length()` which reports bits.
- `pypdfbox/pdmodel/encryption/pd_encryption.py`: added `/CF` dispatch — `get_std_crypt_filter_dictionary` / `set_std_crypt_filter_dictionary`, `get_default_crypt_filter_dictionary` / `set_default_crypt_filter_dictionary`, `get_crypt_filter_dictionary(name)` / `set_crypt_filter_dictionary(name, dict)`, `remove_v45_filters` (strips /CF /StmF /StrF /EFF for downgrade to V<=3). Crypt-filter entries are stored as direct objects per PDFBOX-4436.
- `pypdfbox/pdmodel/encryption/standard_security_handler.py`: `is_aes()` for V=4 now resolves through `/CF/<StmF>/CFM` first (authoritative per spec), falling back to the legacy `/StmF`-name heuristic only when `/CF` is absent — fixes detection on documents that use a custom crypt-filter name. Added `get_stream_filter_name(encryption)` / `get_string_filter_name(encryption)` static helpers.
- `pypdfbox/pdmodel/encryption/pd_encryption.py`: added `get_eff()` / `set_eff()` accessors for the `/EFF` (default embedded-file crypt-filter name) entry — needed by per-object dispatch so embedded files can pick a different cipher than ordinary streams (PDF 32000-1 §7.6.5).
- `pypdfbox/pdmodel/encryption/standard_security_handler.py`: per-object crypt-filter dispatch — `prepare_for_decryption` and `prepare_document` now resolve `/StmF`, `/StrF` and `/EFF` through `/CF` at init time and cache the resulting `/CFM` per slot. `decrypt_string` / `encrypt_string` / `decrypt_stream` / `encrypt_stream` (the last two grew an `is_embedded_file=False` flag) dispatch to RC4 (`V2`), AES-128-CBC (`AESV2`), AES-256-CBC (`AESV3`) or pass-through (`Identity`/`None`) according to the cached routing table. For V<4 documents the routing table stays empty and the legacy single-algorithm path inherited from `SecurityHandler` runs unchanged. Write side: `prepare_document` now installs `/CF/StdCF` + `/StmF` + `/StrF` for V=4 and V=5 outputs so the read side rebuilds the same routing table on reload. `/EFF` is intentionally left absent on writes so embedded files inherit `/StmF` per the spec default.

## Public-key encrypt path

- `pypdfbox/pdmodel/encryption/public_key_security_handler.py`: `prepare_document` now wires the encrypt side. Generates a 20-byte random seed, builds a one-recipient PKCS#7 envelope per `PublicKeyRecipient` via `cryptography.hazmat.primitives.serialization.pkcs7.PKCS7EnvelopeBuilder` (AES-128 / `adbe.pkcs7.s4` for 128-bit policies, AES-256 / `adbe.pkcs7.s5` for 256-bit), serialises the envelopes as DER, populates `/Encrypt` (`/Filter /Adobe.PubSec`, `/SubFilter`, `/V`, `/R`, `/Length`, `/Recipients`, `/CF /DefaultCryptFilter` + `/StmF` + `/StrF`), and derives the file key per §7.6.5 (SHA-256 for V>=5, SHA-1 for V=4) so the existing per-object cipher dispatch works straight away. Constructor accepts an optional `protection_policy` mirroring `StandardSecurityHandler`. Deferred: per-recipient algorithm negotiation, the `/AdbeRecipientInfo` extension that lets one envelope carry many recipients, and multi-recipient policies whose recipients have *different* permission flags (the spec wants one envelope per distinct permission set; we currently emit one envelope per recipient regardless).

## PDFunctionType4 PostScript eval

- `pypdfbox/pdmodel/common/function/pd_function_type4.py`: `eval(input)` now ships per PDF 32000-1 §7.10.5. Body bytes are read through `COSStream.create_input_stream` (so `/Filter` chains decode), tokenised whitespace+brace, parsed into a nested instruction sequence (each `{...}` block becomes a Python `list`), and run through a minimal stack machine. Operator coverage: arithmetic (add/sub/mul/div/idiv/mod/neg/abs/ceiling/floor/round/truncate/sqrt/sin/cos/atan/exp/ln/log/cvi/cvr), stack (dup/exch/pop/copy/index/roll), boolean (eq/ne/lt/le/gt/ge/and/or/xor/not/bitshift/true/false), conditional (if/ifelse). `def`, `forall`, `for`, dictionaries, and other operators not allowed in Type 4 are intentionally rejected (raise `OSError`). Numeric stability uses Python `float`; booleans live on the same stack as numbers per spec and are coerced to `1.0`/`0.0` on the way out for `/Range` clipping. Stack underflow / type mismatch / unknown operator all surface as `OSError` (mirrors upstream `IOException`). Deferred: instruction-sequence caching (we re-parse on every `eval` call), richer error-recovery semantics, and cross-checking numeric edge cases against Java's IEEE-754 single-precision rounding.

## PDResourceCache + PDDocument resource_cache wiring

- `pypdfbox/pdmodel/pd_resource_cache.py` (NEW): abstract `PDResourceCache` (port of upstream `ResourceCache`) plus concrete `DefaultResourceCache` (port of upstream `DefaultResourceCache`). Per-category dicts keyed by indirect `COSObject` ref, identity-cached so repeat lookups round-trip the same wrapper. **Eviction differs from upstream**: PDFBox uses `SoftReference`-keyed maps that the JVM can reclaim under memory pressure; we hold strong refs and expose an explicit `clear()` instead. Subclass and override the `put_*` methods if a workload needs LRU/soft-ref semantics.
- `pypdfbox/pdmodel/pd_document.py`: `get_resource_cache()` now lazily allocates a `DefaultResourceCache` on first call (was `NotImplementedError`). `set_resource_cache(cache)` stores the supplied cache and accepts `None` to disable caching (a subsequent `get_resource_cache` re-allocates a fresh default). `add_signature(...)` shipped in the digitalsignature write-side pipeline (see "PDSignature write-side pipeline" below) — it now stages a `PDSignature` + `SignatureInterface` for the next `save_incremental` call and only raises `TypeError` when the first arg isn't a `PDSignature`. `register_true_type_font_for_closing(font)` becomes a no-op append onto an internal `_fonts_to_close` list — Python GC handles TTF lifetimes, so we don't drive teardown; the list is preserved so future font-subsetting work has a registration log to drain.
- `pypdfbox/pdmodel/pd_resources.py`: typed indirect resource lookups now consult the document resource cache for fonts, XObjects, color spaces, patterns, shadings, graphics states, and property lists. Direct `get_font()` entries and raw `get_xobject()` still return dereferenced COS entries.

## PDStructureElement page wiring + role-map resolution

- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_structure_element.py`: `get_page()` / `set_page(page)` now ship — `/Pg` round-trips through a typed `PDPage` (set accepts a `PDPage` or raw `COSDictionary`; `None` removes the entry). `get_standard_structure_type()` walks the `/P` parent chain to the `StructTreeRoot`, reads its `/RoleMap`, and follows the chain (default → mapped → mapped → ...) until a fixed point. Cycles are bounded by both a `seen` set and a depth cap of 16 hops (mirrors upstream's role-map ceiling and protects against pathological PDFs); the parent walk is similarly capped against malformed cyclic `/P`. Non-name `/RoleMap` values are skipped (cannot resolve to a standard structure type).

## PDFTextStripper — /Differences decode + width-based word spacing

- `pypdfbox/text/pdf_text_stripper.py`: added a typed-font lookup cache that resolves each `Tf` resource name through `PDFontFactory`. When the active font lacks a `/ToUnicode` CMap but is a `PDSimpleFont` (Type1/TrueType/Type3) with an `/Encoding` dictionary, show-text bytes are now decoded via `PDSimpleFont.decode` — which threads each byte through the typed `Encoding` (including `/Differences` overlays) and the Adobe Glyph List. Resolution order: `/ToUnicode` first (most authoritative), then `PDSimpleFont.decode`, then Latin-1 fallback. Mirrors upstream's `PDFont.toUnicode` / `PDSimpleFont.toUnicode` cascade.
- `pypdfbox/text/pdf_text_stripper.py`: per-character horizontal advance now derives from the active font's `/Widths` array when available — `_compute_avg_advance` converts `PDSimpleFont.get_average_font_width()` (thousandths of an em) to user-space units at the current `font_size`. Both the `_emit` cursor advance and `_format_positions` right-edge calculation share this metric, so the word-gap heuristic adapts to wide vs narrow fonts. Falls back to the legacy 0.5-em monospace estimate when the font has no usable `/Widths` (preserves the no-resources test case).
- `pypdfbox/pdmodel/font/pd_simple_font.py`: added `get_average_font_width()` returning the arithmetic mean of non-zero `/Widths` entries in thousandths of an em. Skipping zero-width entries avoids dragging the mean toward zero on sparsely-mapped fonts (most slots are `.notdef = 0`). Mirrors upstream `PDSimpleFont.getAverageFontWidth`.

## tools/merge wave-13 upgrade — import_page + intra-source link remap

- `pypdfbox/tools/merge.py`: pages are now deep-copied via `PDDocument.import_page` instead of being re-parented in place. After each source's pages land in the target, the merger walks each new page's `/Annots` in lockstep with the source page's `/Annots`, and for every `/Subtype /Link` annotation rewrites `/Dest[0]` (and `/A` `/D[0]` when `/A` is a GoTo action) to the imported target page when the original `/D[0]` resolved to a same-source page that was also imported in this batch. Cross-source / out-of-batch references are left as-is (the link will dangle — preferable to silently rewriting it to the wrong page).
- `pypdfbox/tools/merge.py`: supported catalog `/Names` trees (`/Dests`, `/EmbeddedFiles`, `/JavaScript`) are merged into flat output name trees. Duplicate names are preserved by suffixing later entries as `#2`, `#3`, ...; Link annotations that target renamed named destinations are rewritten to the suffixed name. Full `PDFMergerUtility` reconciliation of forms, outlines, structure trees, and destination-module internals remains deferred.

## Encrypted xref-stream round-trip (wave-13 fix)

- `pypdfbox/pdfwriter/cos_writer.py::visit_from_stream`: cross-reference streams (`/Type /XRef`) are now exempted from the per-object encryption pass per ISO 32000-2 §7.6.2 ("All cross-reference streams in the file shall not be encrypted"). The dictionary block is also bracketed with `_in_encrypt_subtree=True` so the embedded `/ID` strings stay cleartext — required because the standard handler folds `/ID[0]` into the file-encryption-key derivation. Previously the writer emitted ciphertext xref-stream bodies, which the parser could not bootstrap (FlateDecode would see ciphertext before any handler was built). Tests: `tests/pdfwriter/test_xref_stream_output.py::test_xref_stream_body_stays_plaintext_when_handler_active` (renamed from `..._is_encrypted_when_handler_active`, inverted assertion to match the spec).
- `pypdfbox/cos/cos_stream.py`: added `set_skip_encryption(bool)` / `is_skip_encryption()` plus an auto-detect branch in `set_security_handler` that flips the skip flag when the stream's dict carries `/Type /XRef`. Closes the gap where the parser marks one xref-stream COSStream during `_handle_xref_stream_at` but a later lazy-loader materialises a different instance from disk — the auto-detect ensures the document-level `PDDocument.decrypt` walk doesn't garble it either way.
- `pypdfbox/pdfparser/pdf_parser.py`: `_handle_xref_stream_at` now (a) marks the xref stream skip-encryption before any decode, (b) decodes the entry table BEFORE running the handler bootstrap (so `/Encrypt`'s xref entry is registered when `_resolve_dict_entry` looks it up), and (c) `_resolve_dict_entry` accepts `XrefType.STREAM` entries (uncompressed — they carry a real byte offset) so the eager bootstrap can resolve `/Encrypt` from a pure-xref-stream document. Previously the resolver only honoured `XrefType.TABLE`, which made the eager bootstrap dormant for every PDF 1.5+ document.
- `pypdfbox/loader.py::Loader.load_pdf`: stages the password on the parser via `parser.set_password(password)` before `parse()` runs. Required for documents whose xref *itself* lives in a stream — the security handler must be ready by the time the bootstrap reaches downstream indirect streams.
- `pypdfbox/pdmodel/pd_page.py::_stream_bytes`: switched from `create_raw_input_stream` to `create_input_stream` so the security handler decrypts and the `/Filter` chain (typically `/FlateDecode` for content streams emitted by our writer) is unwound before the bytes hit `PDFTextStripper`. Returning still-encrypted or still-compressed bytes here masked the bug above for traditional encrypted PDFs in earlier waves.
- `tests/integration/test_end_to_end.py::test_encrypt_and_xref_stream_combined_roundtrip`: removed the wave-12 `pytest.skip` — the parser can now re-decrypt encrypted xref streams, so the round-trip is asserted unconditionally.

## AcroForm field-value APIs

- `pypdfbox/pdmodel/interactive/form/pd_text_field.py`: `get_value` / `set_value` / `get_value_as_string` already shipped — `/V` is a `COSString`. `get_value` returns `""` (not `None`) when absent, matching upstream. Inheritance from `/Parent` is automatic via `PDField.get_inheritable_attribute` per PDF 32000-1 §12.7.4 (so a child with no own `/V` returns the parent's value).
- `pypdfbox/pdmodel/interactive/form/pd_button.py`: `get_value` / `set_value` / `get_value_as_string` already shipped — `/V` is a `COSName` (the on-state name). Inherited unchanged by `PDCheckBox` and `PDRadioButton`. `PDPushButton` overrides the read side to return `""` per upstream (push buttons hold no value); `set_value` is intentionally not overridden so callers don't trip on no-op assignment.
- `pypdfbox/pdmodel/interactive/form/pd_choice.py`: added `get_value_as_string` returning the comma-joined view of the multi-value list (mirrors upstream `PDChoice.getValueAsString`). `get_value` / `set_value` accept single-string or `list[str]` and auto-collapse to `COSString` for length-1 selections vs `COSArray` for multi-select.
- `pypdfbox/pdmodel/interactive/form/pd_signature_field.py`: added `get_value_as_string` returning `""` per upstream — signatures have no single textual representation; consumers wanting metadata should call `get_signature()` and inspect `/Name` / `/Reason` / `/Location`. Typed `PDSignature` wrapping for `get_value` already shipped; full `PDSignature.verify` cluster (signing + verification) remains deferred per PRD §6.10.
- `pypdfbox/pdmodel/interactive/form/pd_non_terminal_field.py`: added `get_value` / `set_value` / `get_value_as_string`. `get_value` returns the raw `COSBase` of own `/V` per upstream (children inherit `/V` lazily through `PDField.get_inheritable_attribute`, so this is intentionally non-recursive — consumers needing an effective value should iterate `get_children()` and call `get_value` on each typed leaf). `get_value_as_string` returns the string form when `/V` is a `COSString` or `COSName`, else `""`.

## PDAcroForm.flatten

- `pypdfbox/pdmodel/interactive/form/pd_acro_form.py`: added `flatten(fields=None, refresh_appearances=False)` per PDF 32000-1 §12.7.5.5. Walks every terminal descendant of the requested fields (defaulting to the whole form), pulls each widget's `/AP /N` (honouring `/AS` when `/N` is a state subdictionary), registers it as a Form XObject under the host page's `/Resources /XObject` (collisions resolved by allocating fresh `Fm<n>` keys), and appends a `q a 0 0 d e f cm /<name> Do Q` snippet to the page's `/Contents` (single-stream `/Contents` is promoted to a 2-element array — no in-place stream rewrite). The CTM maps the form's transformed `/BBox` to the widget's `/Rect`; rotated/skewed `/Matrix` entries are approximated by the axis-aligned bounding box of the transformed corners (full upstream parity would emit a non-uniform CTM). Widgets without a usable `/AP /N` and without a resolvable host page are silently skipped (matches upstream's `hasValidAppearance` guard). Host page resolution prefers the widget's `/P` back-pointer and falls back to scanning every page's `/Annots` when absent. When `fields=None` the catalog's `/AcroForm` entry is removed; otherwise only the named fields are stripped from `/Fields`. `refresh_appearances=True` raises `NotImplementedError` (rebuilding `/AP /N` from `/V` requires per-`/FT` widget-appearance construction — deferred).

## PDSignature write-side pipeline

- `pypdfbox/pdmodel/pd_document.py`: `add_signature(sig, signature_interface, options=None)` now ships. Accepts a typed `PDSignature` plus a `SignatureInterface` callback. Wires the signature dict into `/AcroForm /Fields` inside an invisible signature widget (`/T = "Signature{N}"`, `/Rect = [0 0 0 0]`, `/Subtype /Widget`, `/F = 132`), anchors the widget to the first page's `/Annots`, sets `/SigFlags = 3` (SignaturesExist | AppendOnly), and stages the signer for the next `save_incremental`. Defaults `/Filter Adobe.PPKLite` + `/SubFilter adbe.pkcs7.detached` when caller didn't set them. Does **not** create a new field name slot when one already exists — it picks the next free `Signature{N}`.
- `pypdfbox/pdmodel/pd_document.py`: `save_incremental` now drives the signing pipeline when `add_signature(...)` has staged a pending signature. Pre-flight phase reserves a `/Contents <0×16384>` placeholder (8192 raw bytes, headroom for typical RSA-2048 PKCS#7 detached blobs + chain) and a `/ByteRange [0 9999999999 9999999999 9999999999]` placeholder, runs the writer into a `BytesIO`, locates the placeholder, computes the real `/ByteRange [0 X Y Z]` bracketing everything outside the hex `<…>` (the angle brackets themselves stay inside the hashed range per ISO 32000-1 §12.8.1), splices the real ByteRange in place (trailing-space pad keeps width identical), invokes `signature_interface.sign(io.BytesIO(bracketed_bytes))` to produce the DER PKCS#7 blob, and patches the hex-encoded blob into the `/Contents` slot (zero-pads to placeholder length). The bracketed-bytes math is `(X-0) + (Z-(Y-1)) = file_size - hex_placeholder_len`. Final bytes are drained to the user-supplied `target` (path / `BinaryIO` / `RandomAccessWrite`).
- `pypdfbox/pdmodel/pd_document.py`: `save_incremental_for_external_signing(output) -> ExternalSigningSupport` runs the same pipeline minus the `signature_interface.sign` call. Returns an `ExternalSigningSupport` handle exposing `get_content() -> bytes` (the bracketed bytes the caller hashes + signs, e.g. via an HSM) and `set_signature(pkcs7_der: bytes) -> None` (splices the blob in, drains to `output`). `add_signature` may be called with `signature_interface=None` to use this path.
- `pypdfbox/pdmodel/pd_document.py`: new `ExternalSigningSupport` helper class (no upstream-specific equivalent in pypdfbox — the upstream `org.apache.pdfbox.pdmodel.interactive.digitalsignature.ExternalSigningSupport` is mirrored here as a small dataclass-ish shim).
- `pypdfbox/pdmodel/interactive/digitalsignature/signature_interface.py` (NEW): abstract `SignatureInterface.sign(content) -> bytes` callback contract — port of upstream `org.apache.pdfbox.pdmodel.interactive.digitalsignature.SignatureInterface`.
- `pypdfbox/pdmodel/interactive/digitalsignature/pkcs7_signature.py` (NEW): concrete `Pkcs7Signature(SignatureInterface)` wrapping `cryptography.hazmat.primitives.serialization.pkcs7.PKCS7SignatureBuilder`. Defaults to SHA-256 and emits `[PKCS7Options.DetachedSignature, PKCS7Options.Binary]` so the document bytes stay OUT of the SignedData blob (required for PDF signing per ISO 32000-1 §12.8.3.3) and SMIME-style CRLF rewriting is suppressed. Library-first per CLAUDE.md — no ASN.1 hand-rolling, PyCA wraps OpenSSL.
- `pypdfbox/pdfwriter/cos_writer.py`: new `allow_signing_placeholders=False` constructor flag. When `True`, the safety guard that normally rejects re-saving a source-side signature with a `[0 0 0 0]` ByteRange placeholder (`_reject_signed_with_byterange_placeholder`) is bypassed. `PDDocument`'s signing pipeline sets this because it owns the placeholder + post-write splice; everyone else stays protected against accidentally invalidating an existing signature.

## Type 1 / CFF font program parsing — fontTools-backed

- `pypdfbox/fontbox/type1/type1_font.py` (NEW): thin wrapper over `fontTools.t1Lib.T1Font`. Exposes `Type1Font.from_bytes(raw)` (PDF `/FontFile` stream form — cleartext header + binary eexec + trailing zeros, NOT PFB-wrapped), `units_per_em` (derived as `1 / FontMatrix[0]`), `font_matrix`, `has_glyph(name)`, `get_width(name)` (forces a `.draw()` against `fontTools.pens.basePen.NullPen` to populate the T1CharString's `.width` attribute, then memoises), and `get_path(name)` returning the simple list-of-tuples format `("moveto", x, y)` / `("lineto", x, y)` / `("curveto", x1, y1, x2, y2, x, y)` / `("closepath",)`. fontTools' `T1Font` normally requires a file path — we bypass that by setting `data` + `encoding` directly on a `__new__`-built instance and triggering `parse()` on first dict access. Library-first per CLAUDE.md MEMORY.
- `pypdfbox/fontbox/cff/cff_font.py` (NEW): thin wrapper over `fontTools.cffLib.CFFFontSet`. Exposes `CFFFont.from_bytes(raw)` (PDF `/FontFile3 /Subtype /Type1C` stream form), `font_names`, `name`, `units_per_em` (derived as `1 / FontMatrix[0]`, default 1000 for the standard CFF matrix `[0.001 0 0 0.001 0 0]`), `font_matrix`, `has_glyph(name)`, `get_width(name)` (uses `fontTools.misc.psCharStrings.T2WidthExtractor` with `nominalWidthX` / `defaultWidthX` from the CFF Private DICT — interprets the leading charstring operand as `width - nominalWidthX` per CFF §3.1), and `get_path(name)` in the same list-of-tuples format.
- `pypdfbox/pdmodel/font/pd_type1_font.py`: extended `get_glyph_width(code)` to consult the embedded font program (parsed lazily from `/FontFile` via `Type1Font.from_bytes`) **after** `/Widths` and **before** the Standard 14 AFM fallback. Added `get_glyph_path(code) -> list[tuple]` returning the program's outline for `code` in font units (empty list when no program / unmapped). Added `set_font_program(font: Type1Font | None)` injector mirroring the TTF-side `set_true_type_font` — lets callers that already have the program in hand bypass `/FontFile` parsing, and lets tests skip the byte-level fixture round-trip. Code → glyph-name resolution honours `/Encoding` (with `/Differences` overlay) via the existing `get_encoding_typed()` chain.
- `pypdfbox/pdmodel/font/pd_type1c_font.py`: graduated from a marker subclass to a working CFF-backed font wrapper. Overrides `_program_width(code)` and `get_glyph_path(code)` to source widths and outlines from the embedded `/FontFile3` (`/Subtype /Type1C`) stream via `CFFFont.from_bytes`. `set_font_program(font: CFFFont | None)` injector mirrors PDType1Font. `/Widths` precedence and Standard 14 AFM fallback continue to flow through the inherited `PDType1Font.get_glyph_width`.
- `pypdfbox/pdmodel/font/pd_mm_type1_font.py`: still a pure marker subclass — `set_font_program` / `get_glyph_width` / `get_glyph_path` inherit unchanged from `PDType1Font`. Multiple-Master variant axis interpolation is deferred (PDFBox upstream also delegates to the Type 1 path for these methods).
- Deferred: auto-dispatch of `PDType1CFont` from `PDFontFactory` based on FontDescriptor `/FontFile3 /Subtype /Type1C` inspection (today the wrapper is reachable only via direct construction); CID-keyed CFF (`/Subtype /CIDFontType0C`) parsing through the same CFFFont primitive (planned alongside PDCIDFontType0 enrichment); rendering integration (`PDFRenderer` will start consuming `get_glyph_path` once text ops are wired up — see Rendering cluster #1 deferral note).

## Standard 14 AFM bundling

- `pypdfbox/pdmodel/font/afm/` (NEW): bundles the 14 Adobe Core AFM files (Times-Roman / -Bold / -Italic / -BoldItalic, Helvetica / -Bold / -Oblique / -BoldOblique, Courier / -Bold / -Oblique / -BoldOblique, Symbol, ZapfDingbats) verbatim from `pdfbox/src/main/resources/org/apache/pdfbox/resources/afm/`. Loaded via `importlib.resources` so the lookup works from wheels, editable installs, and source checkouts. License + Adobe redistribution notice in `pypdfbox/pdmodel/font/afm/LICENSE.txt`; root `NOTICE` updated to acknowledge the Adobe Core 14 AFM bundle.
- `pypdfbox/pdmodel/font/afm_loader.py` (NEW, original): thin wrapper over `fontTools.afmLib.AFM`. Exposes `load_standard14(name) -> AfmMetrics` (per-name singleton cache — same instance returned on every call, mirroring upstream `Standard14Fonts.getAFM`), plus `AfmMetrics.get_glyph_width(name) -> float` and `AfmMetrics.get_font_metrics() -> dict` (FontName / FontBBox / ItalicAngle / Ascent / Descent / CapHeight / XHeight / StemV / IsFixedPitch). `StemV` is sourced from AFM `StdVW` (the AFM names for vertical-stem-width).
- `pypdfbox/pdmodel/font/standard14_fonts.py`: per-glyph widths and font-descriptor numerics (FontBBox, ItalicAngle, Ascent, Descent, CapHeight, XHeight, StemV) now load from the bundled AFM via `afm_loader.load_standard14`. The static `_FAMILY_METRICS` dict of placeholder averages is gone; only the `Flags` bits (computed from family heuristics) and the alias map stay static. `get_average_widths(name)` builds the 256-element table by mapping each code through the font's default encoding (StandardEncoding for Times / Helvetica / Courier, SymbolEncoding for Symbol, ZapfDingbatsEncoding for ZapfDingbats per PDF 32000-1 §9.6.2.4) and looking the glyph name up in the AFM. Codes resolving to `.notdef` get `0.0`. The 256-element table is cached per canonical name; callers receive a fresh copy. New `Standard14Fonts.get_afm(name)` and `Standard14Fonts.get_glyph_width(name, glyph_name)` mirror the upstream FontMetrics surface.
- `pypdfbox/pdmodel/font/pd_type1_font.py`: `get_glyph_width(code)` routes through `/Widths` first (PDF 32000-1 §9.6.6.4 — `/Widths` overrides AFM even for the Standard 14), then the embedded font program if any, then the bundled AFM via `Standard14Fonts.get_glyph_width` for the canonical fonts (with optional `/Encoding` overlay), then `0.0`.

## Rendering cluster #1 — Pillow + aggdraw page rasteriser

- `pypdfbox/rendering/pdf_renderer.py` (NEW): `PDFRenderer(document)` mirrors `org.apache.pdfbox.rendering.PDFRenderer`. Subclasses `PDFStreamEngine` and folds the page-drawing logic that upstream splits into `PageDrawer` directly into operator handlers. Backend is **Pillow + aggdraw** — aggdraw provides anti-aliased AGG-quality stroking, native cubic Bezier path segments, and PIL-style `(a, b, c, d, e, f)` affine transforms via `Draw.settransform(...)`. Operators implemented: path construction (`m`, `l`, `c`, `v`, `y`, `re`, `h`), painting (`S`, `s`, `f`/`F`, `f*`, `B`, `B*`, `b`, `b*`, `n`), graphics state (`q`/`Q` push/pop CTM + colour + line width), transform (`cm`), colour (`RG`/`rg`/`G`/`g`/`K`/`k` for DeviceRGB/DeviceGray/DeviceCMYK), line width (`w`), and image XObject `Do` for `/Subtype /Image` (JPEGs via `/Filter /DCTDecode` open through PIL after stop-filtering at DCTDecode; raw 8-bpc DeviceRGB / DeviceGray rasters via `Image.frombytes`). Even-odd fill rule (`f*`/`B*`/`b*`) is implemented by flattening Beziers and XOR-merging per-subpath PIL polygon masks (aggdraw's path fill is non-zero only); the resulting binary mask loses sub-pixel AA on the outer edge, a documented limitation. Initial CTM bakes the PDF→PIL Y-axis flip plus DPI scale plus media-box origin offset into a single 6-tuple. Per-render aggdraw `Draw` is rebuilt after any direct PIL paste so its internal pixel buffer never overwrites pasted-mutation regions on `flush()`. Deferred to text/font cluster: text operators (`BT`/`ET`/`Tj`/`TJ`/`'`/`"`) are silently skipped — real glyph rendering needs the font cluster's `fontTools` glyph paths. Deferred entirely (silent skip): shadings, patterns, transparency groups, soft masks, blend modes, line dash/cap/join, clipping paths, form XObjects, inline images. `render_image(page_index, scale=1.0)` and `render_image_with_dpi(page_index, dpi=72.0)` mirror upstream's two entry points.

## Rendering cluster #2 — text + form XObjects + clip + inline images

- `pypdfbox/rendering/pdf_renderer.py`: extended with four operator families.
  - **Text**: `BT`/`ET`/`Tf`/`Tc`/`Tw`/`TL`/`Tz`/`Ts`/`Td`/`TD`/`Tm`/`T*`/`Tj`/`TJ`/`'`/`"`. The `_GState` dataclass now carries text state (font, font size, text matrix, text line matrix, char spacing, word spacing, leading, rise, horizontal scaling). Glyph outlines for embedded TrueType fonts come through fontTools (`tt.getGlyphSet()[name].draw(pen)`) into a custom `_AggdrawPathPen` that scales coordinates to unit-em and emits aggdraw `Path.curveto`/`lineto`/`moveto`/`close`. Quadratic TT segments are converted to exact cubic Beziers via the standard 1/3 control-point elevation. `qCurveTo`'s implicit-on-curve walk follows fontTools' TT convention (consecutive off-curves get a halfway implicit on-curve). `PDType0Font` with a `CIDFontType2` descendant pulls the descendant's `/FontDescriptor /FontFile2` and reuses the same path. `PDType1Font` / `PDType3Font` / Standard 14 (no embedded program) silently fall back to a faint placeholder rectangle covering the glyph's advance × ~0.7 em — non-fatal, no crash. Per-font lookups are cached on `(id(resources), font_key)` so `Tf` doesn't re-parse the embedded TTF on every text run.
  - **Form XObjects** (`Do` for `/Subtype /Form`): saves the GS, post-multiplies the form's `/Matrix` onto the CTM, intersects the form's `/BBox` into the current clip mask, swaps `_resources` for the form's own `/Resources` if present, then re-feeds the form's content-stream bytes through the engine's dispatch loop via a small `_process_form_bytes` helper (a thin wrapper around `PDFStreamParser` over a `RandomAccessReadBuffer` — the form is not yet a `PDContentStream` subclass).
  - **Clip** (`W` / `W*`): a `_pending_clip` flag is set; the next `n` (or any path-paint operator) builds a polygon mask in device space (Beziers flattened by 16-step sampling, even-odd via `ImageChops.logical_xor` per subpath, non-zero via per-subpath polygon union), intersects it with any existing GS clip via `ImageChops.multiply`, and stores the result on `_GState.clip_mask`. Subsequent paint operators render into a transparent RGBA layer and composite back through `clip_mask`. Image pastes mask their bbox by `(rect_mask × clip_mask)`.
  - **Inline images** (`BI` … `ID` … `EI`): the parser already collapses the triplet into a single `BI` operator carrying both `image_parameters` (the dict) and `image_data` (the raw bytes). `_op_inline_image` synthesises a transient PIL image from those, recognising the abbreviated keys per PDF spec §8.9.7 Table 92 (`W`/`H`/`CS`/`BPC`/`F`) and the abbreviated colour-space + filter names (`G`/`RGB`/`CMYK`, `DCT`/`JPX`/`Fl`/`A85`/`AHx`/`CCF`/`LZW`/`RL`), then routes through the same `_paste_image` path as `Do`-image.
  - **Deferred (silent skip)**: shadings, patterns, transparency groups, soft masks, blend modes, line dash/cap/join, `Tr` text rendering modes (clipping/stroke), Type1/Type3/Standard 14 glyph outlines (placeholder rectangle).

## Rendering cluster #3 — Type1 / Type1C (CFF) text rasterisation

- `pypdfbox/rendering/pdf_renderer.py`: glyph dispatch in `_show_string` / `_draw_glyph` now branches on font kind. The TTF branch (cluster #2) is preserved unchanged. New branch: when the resolved PDFont is a `PDType1Font` (PFB-backed embedded Type 1) or `PDType1CFont` (CFF / Type1C — `/FontFile3 /Subtype /Type1C`), the renderer pulls the per-glyph command tuples from `font.get_glyph_path(code)` (Wave 17 fontTools-backed APIs), feeds them into a small `_build_aggdraw_path_from_commands` helper that converts (`moveto`, `lineto`, `curveto`, `closepath`) into `aggdraw.Path` segments scaled by `1 / units_per_em`, and reuses the same `_fill_aggdraw_path` pipeline as the TTF branch (clip-mask-aware fill onto the device CTM). `units_per_em` is sourced from `Type1Font.units_per_em` / `CFFFont.units_per_em` (typically 1000 for the canonical Type 1 font matrix `[0.001 0 0 0.001 0 0]`). Subclasses of `PDType1Font` (`PDMMType1Font`, `PDType1CFont`) are matched via isinstance and ride the same code path.
- **Standard 14 substitution remains deferred.** When a font names a Standard 14 family (Helvetica, Times-Roman, Courier, Symbol, ZapfDingbats, …) but ships *no* embedded program (no `/FontFile`, no `/FontFile3`), `get_glyph_path` returns `[]` and we still emit the cluster #2 placeholder rectangle. To make the gap explicit, the renderer now logs **once per font** at `DEBUG` (`"Standard 14 ... using placeholder rectangle (Liberation TTF substitution not yet bundled — see CHANGES.md)"`) — tracked on `_warned_standard14_fonts: set[id(font)]`. Two viable substitution paths remain on the table for a future cluster: (a) bundling Liberation TTFs from PDFBox's resources (SIL OFL — permissive but the OFL font-rename clause adds NOTICE complexity), or (b) deferring further until users hit the gap with a real corpus. Trade-off: Liberation gives PDFBox-parity Standard-14 rendering at the cost of ~2 MB of bundled fonts and an OFL attribution row in NOTICE; deferring keeps the wheel lean but means Helvetica/Times/Courier render as faint outline boxes.
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_object_reference.py`: added typed `get_referenced_object()` (resolves `/Obj` to a `PDAnnotation` subclass via `PDAnnotation.create`, or to `PDFormXObject`/`PDImageXObject` for `/Subtype /Form` / `/Image` streams) and `set_referenced_object(obj)` (typed setter accepting a `PDAnnotation`/`PDXObject` wrapper or `None`). Raw `get_obj` / `set_obj` accessors stay for back-compat. Local imports avoid the cos→pdmodel→annotation/XObject cycle.
- `pypdfbox/pdmodel/graphics/state/pd_font_setting.py`: typed wrapper over the ExtGState `/Font` 2-element array. `get_font()` resolves slot 0 via `PDFontFactory.create_font` (returns `None` when slot is absent / not a font dictionary — upstream returns the raw `PDFont` only when `/Subtype` is recognised); `set_font(None)` writes `COSNull.NULL` rather than removing the slot so the array shape stays `[font, size]`. `PDExtendedGraphicsState.get_font_setting` / `set_font_setting` route through it; the existing raw `get_font` / `set_font` / `get_font_size` / `set_font_size` helpers are unchanged and operate on the same underlying COSArray.

## Wave 19 — BlendMode + tagged-content authoring + StructureNode insert_before

- `pypdfbox/xmpbox/pdfa_identification_schema.py` (NEW): typed `PDFAIdentificationSchema` wrapper for the PDF/A Identification XMP schema (`http://www.aiim.org/pdfa/ns/id/`, prefix `pdfaid`). Surfaces `part` (int), `conformance`, `amd`, `rev`, `corr` getters/setters; `set_*(None)` removes the entry. Wired into `DomXmpParser._SCHEMA_REGISTRY` for parser dispatch and `XMPMetadata.get_pdf_identification_schema()` for typed lookup. `get_part()` coerces parser-supplied string values back to `int` (returns `None` on garbage rather than raising).
- `pypdfbox/pdmodel/graphics/blend_mode.py` (NEW): typed `BlendMode` wrapper. 17 named singletons (12 separable per PDF 32000-1 §11.3.5.1 + 4 non-separable per §11.3.5.2 + Compatible-as-Normal alias). `BlendMode.from_cos(base)` accepts COSName (direct lookup) or COSArray of COSNames (returns the first recognised entry, falling back to the array's first name when none match — mirrors the PDF spec's fallback-chain shape). `PDExtendedGraphicsState.get_blend_mode()` now returns `BlendMode | None` (was `COSName | str | None`); `set_blend_mode` accepts `BlendMode | COSName | str | None`. **Behavior change**: `copy_into_graphics_state` now passes a `BlendMode` instance to the target's `set_blend_mode` callback (was: raw `COSName`). Consumers can call `BlendMode.get_cos_name()` for the round-trip name.
- `pypdfbox/pdmodel/pd_page_content_stream.py`: marked-content authoring shipped — `begin_marked_content(tag)` / `begin_marked_content_with_dict(tag, prop_list)` / `end_marked_content()` / `add_marked_content_point(tag)` / `add_marked_content_point_with_dict(tag, prop_list)` emit `BMC` / `BDC` / `EMC` / `MP` / `DP`. The `_with_dict` overloads accept either a typed `PDPropertyList` (auto-registered under `/Resources/Properties` with a fresh `Prop<n>` key — re-uses the existing slot when the same property list is referenced twice), a `COSName`, or a `str` interpreted as a literal property-list key. `show_text(...)` now also accepts `bytes` for callers that have already encoded text through their font's encoder; bytes containing CR/LF/null/non-ASCII are emitted as hex form (`<...>`), the rest as literal-string form (`(...)`). Closes the BMC/BDC/EMC item from the wave-7 deferred list.
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_structure_node.py`: added `insert_before(new_kid, before_kid)` returning `True` on success / `False` when the anchor kid is missing. Single-entry `/K` is auto-promoted to a `COSArray` when the insert lands a second sibling. Anchor matching reuses the same `_same_kid` predicate as `remove_kid` so int MCID anchors compare correctly against `COSInteger` `/K` slots.
- `pypdfbox/pdmodel/common/function/pd_function_type4.py`: added `clear_instruction_cache()` (no upstream counterpart) — defensive escape hatch for callers that mutate the underlying COSStream body via `pd_stream.set_data(...)` after the first `eval`, since pypdfbox now caches the parsed PostScript instruction sequence on the wrapper for shading-renderer hot-path performance.
- `pypdfbox/pdmodel/common/function/pd_function_type0.py`: `/Order = 3` (cubic-spline) interpolation now implemented per PDF 32000-1 §7.10.2 — Catmull-Rom Hermite over 4 surrounding samples per axis with edge-clamped neighbour lookups, folded one dim at a time (4^n samples per output dim, vs 2^n for linear). `/Order` values other than 1 or 3 fall back to linear with a `DEBUG` log line. Closes the cubic-spline deferral noted on the original Type-0 sampled-table eval entry.
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_movie.py` (NEW): typed `PDAnnotationMovie` wrapper for `/Subtype /Movie` per PDF 32000-1 §12.5.6.17 Table 184. Surfaces `/T` (title), `/Movie` (raw `COSDictionary` — typed `PDMovie` per Table 271 still deferred), and `/A` (raw `COSBase` — boolean or activation dict; typed `PDMovieActivation` deferred). Wired into `PDAnnotation.create` factory dispatch. `PDActionMovie` now exposes `get_annotation()` returning the typed wrapper and `set_annotation()` accepting `PDAnnotationMovie | COSBase | None`; the legacy raw accessor is preserved as `get_annotation_dictionary()`.
- `pypdfbox/pdmodel/graphics/optionalcontent/pd_optional_content_group.py`: `/Usage` typed accessor pairs added for the four most-used sub-categories per PDF 32000-1 §8.11.4.4 Table 102 — `get/set_usage_{view,print,export}_state`, `get/set_usage_creator`, `get/set_usage_language`, plus `USAGE_STATE_ON` / `USAGE_STATE_OFF` constants. **Behavior change vs upstream**: `set_*(None)` removes the targeted leaf entry and additionally prunes the containing sub-dict and the `/Usage` dict itself when they become empty, so cleared OCGs do not retain empty `/Usage` husks. Reads return Python `str` (uppercase for state names) instead of `COSName`.

## Wave 20 — typed action surfaces

- `pypdfbox/pdmodel/interactive/action/pd_action_launch.py`: full PDF 32000-1 §12.6.4.5 Table 196 surface — `get/set_file` now resolves `/F` through `PDFileSpecification.create_fs(...)` (returns `PDSimpleFileSpecification` for COSString form, `PDComplexFileSpecification` for COSDictionary form — matches upstream `getFile()` typed return); added `get/set_d` (`/D` Solaris/Mac launch command), `get/set_o` (`/O` operation — Win only), `get/set_p` (`/P` parameters — Win only) text-string accessors; `get/set_open_in_new_window` (`/NewWindow` boolean, defaults False when absent); `get/set_win_launch_params` returning the `/Win` sub-dict (Table 197) as raw `COSDictionary`. **Deferred**: typed `PDWindowsLaunchParams` wrapper around the `/Win` sub-dict — callers currently work directly with the `COSDictionary`. **API change vs prior pypdfbox lite surface**: `set_file` previously accepted `str`; now accepts `PDFileSpecification | None`, mirroring upstream. Existing callers wrap their string in `PDSimpleFileSpecification`.

## Wave 22 — 18 parallel agent items: parity round-out across pdmodel/font/text/rendering

- `pypdfbox/pdmodel/documentinterchange/taggedpdf/pd_layout_attribute_object.py`: 12 new accessor pairs reaching ~80% parity with upstream Layout — `b_box`, `block_align`, `border_colors` (polymorphic 3-tuple OR `PDFourColours`), `border_style` (single name OR 4-name array), `border_thickness`, `column_count`, `column_gap`, `column_widths`, `padding`, `inline_align`, `height` / `width` (number OR `"Auto"`). Added enum constants for each name-typed attribute. Pre-existing `get/set_border_color` retained for back-compat.
- `pypdfbox/pdmodel/graphics/image/pd_image_x_object.py`: 9 new accessor pairs — `mask` (typed `PDImageXObject` for stream form, `None` when color-key array), `color_key_mask` (returns `list[int]` for the array form), `soft_mask`, `decode`, `is_interpolate` / `is_image_mask`, `struct_parent` (default `-1`), `metadata` (typed `PDMetadata`), `oc` (typed `PDPropertyList`). Mask vs color-key are mutually exclusive on read.
- `pypdfbox/text/text_position.py`: parity expansion — added `dir`, `text_matrix` fields; new methods `get_x_directional_adj`, `get_y_directional_adj`, `get_x_scale`, `get_y_scale`, `get_height_dir`, `get_dir`, `get_individual_widths`, `get_visible_text`, `contains_diacritic`, `is_diacritic`, `merge_diacritic`, `get_text_matrix`, `get_end_x`, `get_end_y`. All new fields default-initialised — existing `pdf_text_stripper.py` call sites unchanged.
- `pypdfbox/fontbox/ttf/true_type_font.py`: 13 parity accessors — `get_name` (PostScript name, nameID 6), `get_family_name` (1), `get_full_name` (4), `get_version` (5), `get_font_bbox`, `get_italic_angle`, `get_underline_position` / `_thickness`, `is_fixed_pitch`, `get_weight` (`OS/2 usWeightClass`, default 400), `get_width` (default 5), `get_capabilities` (table-presence map), `has_table(tag)`. Delegates to the existing fontTools `TTFont` instance.
- `pypdfbox/pdmodel/graphics/color/pd_color_space.py`: base `get_default_decode(bpc)` declared; `pd_device_gray.py` / `pd_device_rgb.py` / `pd_device_cmyk.py` implement concrete `[0.0, 1.0]` × N component arrays. `get_initial_color` was already declared abstract.
- `pypdfbox/pdmodel/interactive/action/pd_action_embedded_go_to.py`: 6 upstream-named accessor aliases — `get/set_destination` (alias of `get/set_d`), `get/set_open_in_new_window` (`/NewWindow`, default `False`), `get/set_target_directory` (alias of `get/set_target`). Legacy `get_d` / `set_d` / `is_new_window` / `set_new_window` / `get_target` / `set_target` retained.
- `pypdfbox/pdmodel/interactive/form/pd_field_factory.py`: full `/FT` dispatch was already implemented (`/Tx → PDTextField`, `/Btn` with bit 17 → `PDPushButton`, bit 16 → `PDRadioButton`, else `PDCheckBox`, `/Ch` with bit 18 → `PDComboBox` else `PDListBox`, `/Sig → PDSignatureField`); 13 new dispatch tests added. CHANGES.md prior wave-7 wording calling out "all terminal fields currently return PDFieldStub" was stale.
- `pypdfbox/pdmodel/font/pd_type3_font.py`: 13 accessors round-out — `get_char_proc(name)` single-glyph lookup, redesigned `get_font_matrix() -> list[float]` with default `[0.001, 0, 0, 0.001, 0, 0]` per PDF 32000-1 §9.2.4, typed `get/set_font_bbox` returning `PDRectangle`, plus inherited `get_first_char` / `get_last_char` / `get_widths` / new `set_widths` / `set_first_char` / `set_last_char`. **Behavior change**: `get_font_matrix` no longer returns `COSArray | None`; legacy test updated.
- `pypdfbox/pdmodel/graphics/color/pd_lab.py`: confirmed already-shipped `get/set_white_point` / `_black_point` / `_range` per §8.6.5.4; added 7 explicit parity tests.
- `pypdfbox/pdmodel/interactive/documentnavigation/destination/pd_destination_name_tree_node.py`: now subclasses generic `PDNameTreeNode[PDPageDestination]` (closes the wave-19 deferred adoption note). `get_value(name)` traverses balanced `/Kids` trees; `get_names()` returns `dict[str, PDPageDestination]` (was `list[str]`); `set_value(name, dest|None)` and `names()` retained as back-compat shims. Legacy test updated.
- `pypdfbox/pdmodel/documentinterchange/taggedpdf/pd_user_attribute_object.py`: 4 upstream-named methods — `get_owner_properties` (alias of legacy `get_property`), `set_owner_properties`, `add_owner_property(name, value, formatted=None, hidden=False)`, `remove_owner_property(name)`. `/H` is omitted when False.
- `pypdfbox/pdmodel/font/pd_simple_font.py`: 10 PDF-32000-1 §9.8.1 flag accessors — `is_symbolic` / `is_italic` / `is_bold` / `is_fixed_pitch` / `is_serif` / `is_script` / `is_force_bold` / `is_all_cap` / `is_small_cap` / `is_standard_14`. `is_bold` is derived from `/FontWeight ≥ 700` (PDF spec uses /FontWeight, not a /Flags bit; `is_force_bold` is the actual /Flags bit 19).
- `pypdfbox/pdmodel/interactive/form/pd_terminal_field.py`: typed `get_widgets() -> list[PDAnnotationWidget]` (was raw `list[COSDictionary]`), `set_widgets(widgets)` writes `/Kids` and wires each widget's `/Parent` back; `set_actions(typed)` typed override; `apply_change()` / `construct_appearances()` no-op + debug log (appearance regeneration deferred). **Consumer fix-ups**: `pd_acro_form.py::flatten` and `pd_variable_text.py::set_default_appearance` now call `widget.get_cos_object()` to drill down to the COSDictionary.
- `pypdfbox/pdmodel/font/pd_cid_font.py`: 10 parity accessors — `is_embedded`, `get_program` (decodes the first present FontFile/2/3 stream via PDStream.create_input_stream), `get_bounding_box` (PDRectangle from /FontDescriptor /FontBBox), `get_average_font_width` (mean of positive /W or /DW), `has_glyph`, `get_displacement`, `get_position_vector`, `get_height`, `code_to_cid` (identity at base; subclasses do real mapping).
- `pypdfbox/pdmodel/graphics/optionalcontent/pd_optional_content_group.py`: added `get_intent() / set_intent(value)` — `/Intent` accepts string or `list[str]`, defaults to `"View"`. The pre-existing `get_intents()` plural form (returning `list[COSName]`) is preserved alongside.
- `pypdfbox/pdmodel/pd_page_tree.py`: confirmed already-shipped `index_of`, `add`, `insert_after`, `insert_before`, `remove`; 6 new parity tests added.
- `pypdfbox/pdmodel/interactive/action/pd_action_go_to.py`: typed `get_destination` / `set_destination` — `/D` returns `PDDestination` for COSArray, `str` for COSString or COSName (named destination form). `set_destination` accepts `PDDestination | str | None`. **Behavior change**: previously named-destination forms returned a `PDNamedDestination` wrapper; now they return a plain `str`. Sole in-tree consumer (`PDOutlineItem.find_destination_page`) gates on `isinstance(d, PDPageDestination)` and is unaffected.
- `pypdfbox/rendering/pdf_renderer.py`: 7 upstream-named API additions — `is_page_image_with_annotations(page_index)`, `set_subsampling_allowed` / `is_subsampling_allowed`, `set_default_destination` / `get_default_destination` (default `"View"`), `set_image_downscaling_optimization_threshold` / `get_image_downscaling_optimization_threshold` (default `0.5`). All store-only flags — render loop unchanged.
- `pypdfbox/pdfparser/pdf_parser.py`: 4 parity accessors — `get_document()` returns the parsed `COSDocument` (None before parse), `get_pd_document()` lazy-builds + caches a `PDDocument` wrapper, `set_lenient(b)` / `is_lenient()` toggle (default True; pypdfbox already runs lenient).
- `pypdfbox/pdmodel/graphics/shading/pd_shading_type6.py` + `pd_shading_type7.py`: typed `get_decode() -> list[float] | None` (was raw COSArray), `get_function() -> PDFunction | None` (dispatched through `PDFunction.create`); setters accept iterables / typed wrappers / None. `get_bits_per_*` now returns `-1` when absent (matches upstream Java `int` default).

## Wave 23 — 20 parallel agent items: deeper parity round-out (font + cos + io + xmpbox + tagged)

- `pypdfbox/pdmodel/font/pd_font.py`: 8 base parity accessors — `is_embedded` (checks descriptor for FontFile/2/3), `is_damaged` (default False), `get_widths`, `get_first_char`, `get_last_char`, `get_average_font_width`, `get_space_width`, `is_subset` (regex `^[A-Z]{6}\+` on /BaseFont). Subclass overrides take precedence via MRO.
- `pypdfbox/pdmodel/font/pd_type0_font.py`: 9 parity methods — `get_cmap`, `get_to_unicode_cmap`, `code_to_cid`, `code_to_gid`, `read_code` (delegates to active CMap), `get_glyph_width`, `is_vertical` (WMode==1), `to_unicode`, `is_embedded`. Lazy CMap caches.
- `pypdfbox/pdmodel/font/pd_type1_font.py`: 8 upstream-named accessors — `get_base_font`, `get_font_program`, `get_glyph_name_for_code`, `get_path(name)`, `is_embedded`, `is_damaged`, `get_height`, `get_displacement`, `get_average_font_width` (overrides base — `/Widths` first, then bundled AFM mean for Standard 14, then 0.0), `get_standard_14_font_metrics`. **Behavior change**: empty `/Widths` on a Standard 14 font no longer returns 0.0; it returns the AFM mean.
- `pypdfbox/pdmodel/font/pd_true_type_font.py`: 8 parity accessors — `get_base_font`, `get_true_type_font` (renamed from `_get_true_type_font`; underscore alias kept), `get_glyph_name_for_code`, `get_path(name)`, `get_glyph_path(code)`, `get_displacement`, `get_height` (from `glyf` bbox or BoundsPen fallback), `code_to_gid` (symbolic shortcut + encoding/cmap path).
- `pypdfbox/pdmodel/font/pd_cid_font_type0.py`: CID-keyed CFF support — `get_cff_font` (lazy-parses /FontFile3 via `CFFFont.from_bytes`, with positive+negative caching), `set_cff_font` injector, `get_glyph_width(cid)` overrides base with /W → CFF program → /DW resolution order (CFF advance scaled by `1 / units_per_em` to thousandths-em), `get_glyph_path(cid)` via `cidNNNNN` glyph names, `is_cff_embedded` strict-form check, `is_embedded` overrides base. Closes the CID-keyed CFF deferred note from wave 17.
- `pypdfbox/pdmodel/font/pd_cid_font_type2.py`: 9 parity accessors — `get_cid_to_gid_map_bytes` (decoded stream payload, None for /Identity), `is_identity_cid_to_gid_map`, `get_true_type_font` (lazy-parses /FontFile2 via fontbox, cached), `set_true_type_font`, `is_embedded` (narrowed to /FontFile2 only — Type2's only legal embedding form), `has_glyph` (TTF-aware override), `get_glyph_path` (cid→gid → fontTools `glyph_set[name].draw(pen)`), `code_to_cid` identity. Naming deviation: `get_cid_to_gid_map_bytes` (not `get_cid_to_gid_map`) so the parent's existing `get_cid_to_gid_map() -> COSStream | str | None` accessor is preserved for back-compat.
- `pypdfbox/text/pdf_text_stripper.py`: 17 PDFBox-named extraction options — `set/is_sort_by_position` (wired into `_format_positions`), `set/is_suppress_duplicate_overlapping_text`, `is_should_separate_by_beads`, `set/get_drop_threshold`, `set/get_indent_threshold`, `set/get_spacing_tolerance`, `set/get_average_char_tolerance`, `set/get_add_more_formatting`, `set/is_lenient_stream_parsing`. Mostly inert holders for upstream-API parity; `sort_by_position` actually changes emit order. `end_page` default kept at `sys.maxsize` and `paragraph_end` kept at `"\n"` for back-compat.
- `pypdfbox/contentstream/pdf_stream_engine.py`: 11 parity entry points — `get_resources`, `get_current_page`, `get_level`, `is_processing_page`, `set_resources`, `process_form` (alias for `process_stream`), `process_tiling_pattern` (delegates), `process_type3_stream` (delegates), `save_graphics_state` / `restore_graphics_state` / `transform_text` (no-op placeholders for renderer subclass override). Added `_resources_stack` field; `process_stream` now pushes a level + only swaps resources when the nested stream actually carries its own.
- `pypdfbox/pdfparser/base_parser.py`: 4 parity additions — `peek` (alias `peek_byte`), `read` (alias `read_byte`), `unread` (alias `unread_byte`), `read_string` (reads tokens until whitespace; new method matching upstream `BaseParser.readString()`).
- `pypdfbox/fontbox/cmap/cmap_parser.py`: 4 parity additions — `parse_unicode_cmap(bytes)`, `get_cmap_for_name(name)` classmethod with module-level cache, `add_codespace_range(cmap, low, high)` static helper, `parse_chunk(source, cmap=None)` for fragment merging via `use_cmap`.
- `pypdfbox/pdmodel/graphics/color/pd_icc_based.py`: 10 parity accessors — `get_pd_stream`, `get_n` (default 0 per upstream), `set_n`, `get_alternate_color_space` / `set_alternate_color_space` aliases, `get_range_for_component(n)` (default `(0.0, 1.0)`), `set_range_for_component(n, low, high)` (auto-pads intermediate slots), `get_metadata` now returns `PDMetadata` (was raw COSStream — legacy test in `test_pd_additional_color_spaces.py` updated), `set_metadata`, `get_iccprofile_bytes` decoded body.
- `pypdfbox/pdmodel/graphics/pd_x_object.py`: 3 parity accessors — `get_subtype()`, `get_metadata() -> PDMetadata | None`, `set_metadata(value)`. Lazy import of PDMetadata to avoid cycle.
- `pypdfbox/xmpbox/xmp_schema.py`: 9 unqualified-property aliases — `add_unqualified_text_property`, `get_unqualified_text_property`, `add_unqualified_array(name, type)` (with `UNORDERED_ARRAY` / `ORDERED_ARRAY` / `ALTERNATIVE_ARRAY` constants), `add_unqualified_bag_value`, `get_unqualified_array_list`, `get_unqualified_property`, `add_property(prop)` (accepts `(name, value)` tuple or `AbstractField`-like duck), `get_about_attribute()`, `get_about_value()`. Did not duplicate already-present setters.
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_attribute_object.py`: 7 parity additions — `get_revision_number` (`/R`, default 0), `set_revision_number` (rejects negatives), `get_structure_element` / `set_structure_element` back-pointer accessors, plus `notify_change` / `add_to_structure_element` / `remove_from_structure_element` no-op stubs (upstream's structure-element change notification is deferred).
- `pypdfbox/fontbox/type1/type1_font.py`: 12 parity accessors — `get_name`, `get_family_name`, `get_full_name`, `get_weight`, `get_italic_angle` (string-coercing, default 0.0), `get_is_fixed_pitch` (accepts bool/`"true"`/`"false"`), `get_underline_position` / `_thickness`, `get_font_bbox` (4-tuple), `get_encoding` (resolves `"StandardEncoding"` named ref + falls back to StandardEncoding for all-`.notdef` arrays), `get_char_strings_subroutines_charset`, `get_subrs`. Lazy `_meta_cache` + safe defaults.
- `pypdfbox/fontbox/cff/cff_font.py`: 13 parity accessors — `get_name`, `get_top_dict`, `get_private_dict`, `get_charset`, `get_num_char_strings`, `get_global_subrs` / `get_local_subrs` / `get_subrs` (alias), `is_cid_font` (ROS check), `get_property`, `get_default_width_x`, `get_nominal_width_x`, `get_glyph_widths` (batch view). **Drive-by fix**: `get_width` now passes `priv.Subrs` to `T2WidthExtractor` so widths from CFF charstrings using `callsubr` decode correctly (was returning 0).
- `pypdfbox/pdmodel/font/standard14_fonts.py`: 7 PDFBox-named static methods — `contains_name` (snake_case canonical; legacy `containsName` alias kept), `is_standard_14` upstream alias, `get_mapped_font_name` snake_case (legacy `getMappedFontName` alias kept), `get_names() -> set[str]`, `get_aliases() -> dict[str, str]`, `get_font_metrics(name)` raw AFM-derived numerics dict. Internal callers updated to use snake_case.
- `pypdfbox/loader.py`: 5 parity entry points — `load(source, password=None)` upstream alias, `load_pdf_from_bytes(data, password=None)` with type-check guard, `load_pdf_from_file(path, password=None)` accepts str or Path, `load_xfdf` / `load_fdf` raise NotImplementedError with clear deferral messages (so callers get a graceful failure mode instead of AttributeError).
- `pypdfbox/io/scratch_file.py`: 5 parity additions — `create_buffer_with_data(data)` (creates buffer pre-populated + seeked to 0; accepts bytes/bytearray/memoryview), `enqueue_page(page)` / `dequeue_page()` inert no-ops with debug logs (stdlib backing has no page-pool concept), `get_main_memory_max_pages()` returns -1, `get_max_main_memory_bytes()` returns the spill threshold.
- `pypdfbox/cos/cos_boolean.py` + `cos_name.py` + `cos_float.py`: small leaf-primitive parity additions — `COSBoolean.get_boolean(value)` classmethod factory + `is_true` / `is_false`; `COSName.FIRST_CHAR` / `LAST_CHAR` / `WIDTHS` constants; `COSFloat.get_value()` alias.
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_polygon.py` + `pd_annotation_polyline.py`: 5 typed accessors per type — `get/set_interior_color(rgb)` (3-tuple), `get/set_border_style` (typed `PDBorderStyleDictionary`), `get/set_border_effect` (raw `/BE`), `get/set_measure` (raw `/Measure`), polyline-only `get/set_line_ending_styles(start, end)` (`/LE`).

## Wave 24 — 18 parallel agent items: upstream-named aliases + parity round-out

- `pypdfbox/pdfparser/cos_parser.py`: 21 upstream-name aliases — `parse_cos_string` / `parse_cos_name` / `parse_cos_number` / `parse_cos_object_reference`, `parse_object_dynamically(obj, gen, requires_existing_not_compressed)` (routes through bound document pool), `parse_object_stream` (deferred placeholder), `is_eof` / `peek` / `unread` super pass-throughs, `get/set_xref_offset`, `get_document`, `set/is_lenient` (default True), plus deferred placeholders for `parse_xref_object_stream` / `parse_xref_table` / `parse_pdf_header` / `bf_search_for_objects` / `bf_search_for_xref` (raise NotImplementedError pointing at PDFParser).
- `pypdfbox/pdfwriter/cos_writer.py`: 14 parity additions — `get_x_ref_entries`, `get_started_streams`, `set_pdf_version(major, minor)` / `get_pdf_version`, `write_object` / `write_header(version=None)` / `write_xref` / `write_trailer(doc)` aliases, `get_object_number` / `get_generation_number` (raise KeyError on unassigned), `add_signature` no-op hook, `to_hex_string` static, `release` alias. `_do_write_header` now honours `_pdf_version` so a pre-`write()` `set_pdf_version` call pins the header.
- `pypdfbox/pdmodel/encryption/standard_security_handler.py`: 8 parity additions — `is_encrypt_meta_data` alias, `get/set_protection_policy`, `compute_revision_number(key_length, prefer_aes)` static algo selector, `compute_user_password` / `compute_owner_password` / `compute_encrypted_key` public aliases, `is_user_password` / `is_owner_password` (dual-path r2-r4 + r5-r6).
- `pypdfbox/pdmodel/encryption/security_handler.py`: 12 base parity additions — `get/set_current_access_permission`, `get/set_decryption_material`, `is/set_decrypt_metadata` (default True), `decrypt_data` / `encrypt_data` (accept bytes / bytearray / memoryview / file-like via `_coerce_to_bytes` helper), `compute_encrypted_key` / `compute_user_password` / `compute_owner_password` placeholders.
- `pypdfbox/xmpbox/dom_xmp_parser.py`: 8 parity additions — `parse_input(source)` alias, `set/is_strict_parsing` flag (deferred), `set/is_throw_exception_on_invalid_xmp` shared-state aliases, `get_namespace_table()` returning prefix→URI dict, `parse_describe_element` / `parse_property` upstream-named aliases.
- `pypdfbox/xmpbox/xmp_metadata.py`: 18 parity additions — `set_xpacket_begin/id/bytes/encoding`, `is/set_read_only` (synced with end_xpacket marker), `get/set_about`, `remove_schema`, `add_dublin_core_schema` / `add_xmp_basic_schema` / `add_pdfa_identification_schema` / `get_pdfa_identification_schema` typed factories + getters, `get_pdf_schema` / `add_pdf_basic_schema` / `add_pdfa_extension_schema` / `get_pdfa_extension_schema` placeholders.
- `pypdfbox/pdmodel/common/function/pd_function.py`: 9 parity additions — `get_domain_for_input(n)` / `get_range_for_input(n)` (alias for upstream's `getRangeForInput` misnomer) / `get_range_for_output(n)`, `eval_function` alias, `is_function_type_0/2/3/4` predicates, `to_array(numbers)` static helper.
- `pypdfbox/pdmodel/interactive/form/pd_acro_form.py`: 12 parity additions — `get/set_default_resources`, `get/set_default_appearance` (returns `""` not None — matches upstream), `get/set_q` (default 0), `get/set_calc_order`, `is/set_append_only` upstream-named aliases over existing `is_appendonly/set_appendonly`, `refresh_appearances(fields=None)` / `import_fdf` / `export_fdf` NotImplementedError placeholders, `get_xfa` alias for `xfa()`.
- `pypdfbox/pdmodel/encryption/public_key_security_handler.py`: 7 parity additions — `get/set/has_protection_policy`, `get_recipients` / `add_recipient` (lazy-instantiates PublicKeyProtectionPolicy when needed), `compute_seed_value` (20-byte os.urandom), `derive_file_key(seed, recipient_blobs, version, key_length, encrypt_metadata)` extracted §7.6.5 hash composition (SHA-1 for V<5, SHA-256 for V>=5, optional 0xFF*4 suffix when metadata unencrypted).
- `pypdfbox/pdmodel/pd_document.py`: 5 parity additions — `get_page(index)`, `get_signature_fields()` / `get_signature_dictionaries()` (depth-first walk through AcroForm), `requires_full_save()` (True for fresh-or-clean docs), `is_locked_by_outline_destinations()` placeholder.
- `pypdfbox/pdmodel/pd_document_catalog.py`: 11 parity additions — `get/set_outlines` aliases, `get/set_view_preferences` aliases for `get/set_viewer_preferences`, `get_threads()` placeholder returning `[]`, `get/set_perms` / `get/set_legal` / `get/set_collection` raw-dict accessors.
- `pypdfbox/pdmodel/pd_page.py`: 3 parity additions — `get_content_streams()` (list-form `/Contents` accessor), `get/set_struct_parents` (default -1).
- `pypdfbox/pdmodel/pd_document_information.py`: 17 hand-written parity tests (no source change — already had full upstream-named surface).
- `pypdfbox/pdmodel/graphics/color/pd_color_space.py`: 8 base parity additions — `to_rgb_image` / `to_raw_image` placeholders, `get_java_color_space()` returns None, `is_pattern` / `is_indexed` / `is_separation` / `is_device_n` predicates (lazy isinstance checks), `get_array()` raw COSArray accessor.
- `pypdfbox/pdmodel/font/pd_font_descriptor.py`: 25 parity additions — full /Flags bit predicates (`is/set_serif/symbolic/script/non_symbolic/all_cap/small_cap/force_bold` plus existing fixed_pitch/italic), generic `get_flag(bit)` / `set_flag(bit, value)` helpers, missing numeric metric setters (`set_stem_h`, `get/set_avg_width` / `max_width` / `missing_width` / `leading`), `get_font_bounding_box() -> PDRectangle | None` typed.
- `pypdfbox/pdmodel/interactive/documentnavigation/outline/pd_outline_item.py`: 11 hand-written parity tests (no source change — full upstream surface already present per wave 8).
- `pypdfbox/pdmodel/pd_viewer_preferences.py`: 8 parity additions — `is_hide_toolbar` / `is_hide_menubar` / `is_hide_window_ui` / `is_fit_window` / `is_center_window` / `is_display_doc_title` / `is_pick_tray_by_pdf_size` boolean accessors, `get/set_direction` upstream-named aliases. Plus 13 string constants for enum-valued options (NON_FS_*, DIRECTION_*, PRINT_SCALING_*, DUPLEX_*, BOUNDARY_*).
- `pypdfbox/filter/filter.py` + `pypdfbox/filter/filter_factory.py`: 6 parity additions — `Filter.decode_result` static convenience constructor, `Filter.get_decode_params(parameters, index)` static helper resolving /DecodeParms or /DP, `Filter.is_decompression_input_size_known()` (default True), `FilterFactory.INSTANCE` singleton, `FilterFactory.get_filter(name)` / `get_filter_by_short_name(short_name)` (Fl/Ah/etc. abbreviations).
- `pypdfbox/pdmodel/graphics/form/pd_form_x_object.py`: 10 parity additions — `get/set_struct_parents` (default -1), `get/set_oc` typed PDPropertyList, `get/set_group/ref/pieceinfo` raw COSDictionary, `get/set_last_modified` typed datetime via `_parse_pdf_date` / `_format_pdf_date`, `get/set_name`.
- `pypdfbox/pdmodel/interactive/documentnavigation/destination/*`: 15 hand-written parity tests for PDPageXYZDestination + PDPageFitHeight/Width/Rectangle (no source changes — full upstream surface already present, including upstream-correct `FitV`/`FitH` literal mismatch in PDPageFit{Height,Width}Destination.TYPE).
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_structure_element.py`: 27 hand-written parity tests (no source change — full upstream surface present).

## Wave 25 — 20 parallel agent items: deeper parity round-out (font/cos/graphics/annotation)

- `pypdfbox/pdmodel/font/pd_type1c_font.py`: 11 parity additions — `get_cff_font` / `get_cff_type1_font` aliases, `is_embedded` (overrides parent: only checks `/FontFile3`), `is_damaged` (true only when CFF parse fails), `get_path(name)` name-keyed + `has_glyph(name)` charset check, `code_to_name` / `code_to_gid` encoding-driven lookups, `get_units_per_em` (default 1000), `get_height(code)` cached glyph bbox, `get_average_font_width` override (/Widths → CFF `defaultWidthX` → AFM → 0.0). **Deviation from upstream**: upstream hardcodes 500 for unknown average; we fall back to AFM/Standard14 metrics first.
- `pypdfbox/pdmodel/pd_page_labels.py` + `pd_page_label_range.py`: 4 parity additions — `find_label_range_containing(page_index)` (greatest start_index <= page_index), `get_number_of_pages` / `set_number_of_pages` overrides (validates non-negative), `format_label_index(index)` alias for `compute_label_for_offset`.
- `pypdfbox/pdfparser/pdf_stream_parser.py`: 5 parity additions — `get_tokens()` eager drain, `parse_stream()` alias, `is_in_inline_image()` predicate (checks `_inline_image_depth`), `seek_to(offset)` / `get_position()` source-position aliases.
- `pypdfbox/pdmodel/graphics/shading/pd_shading_type3.py`: typed accessors aligned with wave-22 PDShadingType6/7 pattern — `get_coords` / `set_coords` (`/Coords` 6-element required), `get_domain` / `set_domain` returning typed COSArray with default `[0.0, 1.0]`, `get_function` / `set_function` typed via `PDFunction.create()`, `get_extend` / `set_extend(start, end)` typed `tuple[bool, bool]` with default `(False, False)`.
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/revisions.py`: 10 parity additions — `set_object_at` / `set_revision_number_at` setters with bounds + non-negative validation, `is_empty` / `clear` / `contains` / `index_of` / `remove_at` (returns removed obj), `from_cos_array` classmethod parser, `iterator` alias, `__iter__` / `__len__` Pythonic protocols.
- `pypdfbox/fontbox/encoding/glyph_list.py`: 6 parity additions — `get_default()` classmethod alias, `name_to_code_points(name)` alias preserving multi-codepoint sequences, `code_point_to_name` / `code_point_to_string` reverse lookups (lazily-built reverse map under existing `_cache_lock`), `is_unicode_lookup(name)` regex test for `uniXXXX` / `uXXXXXX` patterns, `get_or_unicode_lookup(name)` convenience with synthesized fallback.
- `pypdfbox/cos/cos_null.py` + `cos_integer.py`: 9 parity additions — COSNull `write_pdf` / `get_value() -> None` / `is_null(value)` static / `__bool__` False / `__eq__` / `__hash__` (Python None intentionally NOT equal); COSInteger `double_value` / `get_value` aliases / `equals` / `compare_to` returning -1/0/1 with TypeError on wrong type.
- `pypdfbox/pdmodel/graphics/pd_line_dash_pattern.py`: 5 parity additions — `set_dash_array` / `set_phase` setters, `is_solid()` (empty array), `is_zero_pattern()` (all entries 0), `to_cos_array()` alias.
- `pypdfbox/pdmodel/interactive/documentnavigation/outline/pd_outline_node.py` + `pd_document_outline.py`: 5 parity additions — `set_open_count(count)` public setter, `iterator()` fresh forward iterator, `nodes()` alias for `children()`, `PDDocumentOutline.is_open()` (default True for absent /Count), `open_node` / `close_node` overrides with `is_open()` semantics.
- `pypdfbox/pdmodel/pd_document_name_dictionary.py`: 2 additions (`get_ap` / `set_ap` raw `/AP`) + 29 parity tests covering all 10 `/Names` sub-dictionaries.
- `pypdfbox/pdmodel/graphics/color/pd_color.py`: 6 parity additions — `set_components(values)` defensive-copy setter, `get_color_space_name()`, `to_rgb_image` / `to_raw_image` NotImplementedError placeholders, `__eq__` / `__hash__` value equality. `is_pattern()` extended: now also returns True when color space is PDPattern.
- `pypdfbox/pdmodel/graphics/color/pd_cal_gray.py` + `pd_cal_rgb.py`: confirmed full upstream surface already present (matches wave-22 PDLab pattern); 17 hand-written parity tests added.
- `pypdfbox/pdmodel/graphics/pattern/*`: typed surface upgrade — `PDAbstractPattern.get/set_ext_g_state` typed `PDExtendedGraphicsState`, `is_tiling_pattern` / `is_shading_pattern` predicates; `PDTilingPattern` `PAINT_TYPE_*` and `TILING_TYPE_*` constants, `get/set_b_box` typed `PDRectangle` (was raw COSArray); `PDShadingPattern.get_shading` typed `PDShading` (was raw COSBase). One legacy test assertion swapped (`is raw_shading` → `get_cos_object() is raw_shading`).
- `pypdfbox/pdmodel/font/encoding/encoding.py` + `dictionary_encoding.py`: 4 parity additions — `Encoding.get_name()` polymorphic via overload (no-arg form returns encoding identifier; `get_name(code)` retains glyph-name semantics), `to_glyph_name(code)` alias; `DictionaryEncoding.set_base_encoding(value)` accepts Encoding/COSName/str/None, `set_differences(values)` extended to accept `dict[int, str]` (sorted + run-coalesced into canonical PDF compact form), `to_cos_object` alias.
- `pypdfbox/pdmodel/common/pd_stream.py`: 8 parity additions — `get_cos_stream` / `get_stream` aliases, `set_length(length)` placeholder, `is_filter_undefined()`, `get_file` / `set_file` typed `PDFileSpecification`, `get_file_filters()` / `get_file_decode_parms()` `/FFilter` and `/FDecodeParms` chains.
- `pypdfbox/pdmodel/common/filespecification/pd_complex_file_specification.py`: 3 missing platform-string setters added (`set_file_dos`, `set_file_mac`, `set_file_unix` for `/DOS`, `/Mac`, `/Unix`). Other PDFileSpecification family files already had full parity surface.
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_mark_info.py`: 3 upstream-name aliases — `uses_user_properties()` (PDFBox `usesUserProperties` not `isUserProperties`), `is_suspect()` (PDFBox `isSuspect` not `isSuspects`), `set_suspect(value)`. Pre-existing `is_suspects` / `set_suspects` retained for back-compat.
- `pypdfbox/pdmodel/interactive/documentnavigation/destination/pd_page_destination.py`: 2 parity additions — `get_cos_array()` upstream alias, `set_page(page)` broadened to accept `PDPage` (or any wrapper exposing `get_cos_object()`).
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation_link.py`: 3 parity additions + typed upgrade — `get_destination` / `set_destination` typed dispatch via `PDDestination.create()` (also accepts bare str for named destinations), `get_quad_points()` return-type changed from `COSArray` to `list[float]` (matches upstream `float[]`), `get_p_a` / `set_p_a` for `/PA` previewer action, `get_url_uri()` URL extraction convenience. One legacy test assertion swapped to value-equality.
- `pypdfbox/pdmodel/interactive/annotation/pd_annotation.py`: 8 parity additions — `set_subtype(value)`, `get_rect()` alias, extended `set_color()` to accept `list[float] | tuple[float, ...]`, `get_appearance_state` / `set_appearance_state` (`/AS` name), `get_p` / `set_p(page)` (`/P` parent-page back-pointer with `get_cos_object()` unwrap), `get_struct_parent` / `set_struct_parent` (default -1), `get_optional_content` / `set_optional_content` typed via `PDPropertyList.create()`.
- `pypdfbox/pdmodel/interactive/form/pd_field.py`: confirmed already has the full upstream-named base surface (`is_read_only` / `is_required` / `is_no_export` / `get_alternate_field_name` / `get_mapping_name` / `get_partial_name` / etc.); 14 hand-written parity tests added.

## Wave 26 — 10 parallel agent items: action surface + transition + content-stream operators

- `pypdfbox/pdmodel/interactive/action/pd_action_submit_form.py`: full §12.7.5.2 surface — `get_file()` typed `PDFileSpecification`, `get_url()` (extracts simple form), `get_fields() -> list[PDField] | None` (typed via PDFieldFactory; entries that are bare partial-name strings are skipped — no AcroForm to resolve against), `get_cos_fields()` raw access for back-compat, `get/set_flags`, plus 12 flag-bit predicate pairs per Table 237 (`is_include`, `is_export_format`, `is_get_method`, `is_submit_coordinates`, `is_xfdf`, `is_include_append_saves`, `is_include_annotations`, `is_submit_pdf`, `is_canonical_format`, `is_excl_non_user_annots`, `is_excl_f_key`, `is_embed_form`). **Behavior change**: `get_file()` now returns typed wrapper (was raw `COSBase`); existing test `test_submit_reset_import_hide_and_thread_accessors_round_trip_cos` updated.
- `pypdfbox/pdmodel/interactive/action/pd_action_reset_form.py`: 2 parity additions — `is_include() / set_include(b)` (`/Flags` bit 1: include-set vs exclude-set semantics). Existing `get_fields() -> COSArray | None` preserved for upstream signature parity.
- `pypdfbox/pdmodel/interactive/action/pd_action_import_data.py` + `pd_action_hide.py` + `pd_action_thread.py`: typed `/F` (`PDFileSpecification`) + new typed `get_target/set_target` (Hide), `get_thread/set_thread`, `get_bead/set_bead` (Thread). Legacy `get_t`/`set_t`/`get_d`/`set_d`/`get_b`/`set_b` retained as back-compat aliases. **Behavior change**: `PDActionImportData.get_file()` now returns `PDFileSpecification` (was raw); legacy test updated.
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_structure_tree_root.py`: 2 parity additions — `get_struct_element_for_id(id_string)` (looks up via `/IDTree`), `get_struct_element_for_mcid(page, mcid)` (resolves via `/StructParents` → `/ParentTree` → array indexed by mcid). Other accessors confirmed already complete.
- `pypdfbox/pdmodel/graphics/optionalcontent/pd_optional_content_membership_dictionary.py`: 4 `VISIBILITY_POLICY_*` constants (AllOn/AnyOn/AnyOff/AllOff) + `get_ocgs()` / `set_ocgs(values)` upstream-named aliases (set accepts single-OCG, single-COSDictionary, or list). `get_visibility_policy` / `set_visibility_policy` / `get_visibility_expression` / `set_visibility_expression` confirmed already complete.
- `pypdfbox/pdmodel/interactive/pagenavigation/pd_transition.py`: 4 motion/dimension constants (`MOTION_INWARD`, `MOTION_OUTWARD`, `DIMENSION_HORIZONTAL`, `DIMENSION_VERTICAL`) + `get_scale` / `set_scale` aliases for `/SS` (alongside existing `get_fly_scale` / `set_fly_scale`) + `is_fly_area_to_show` boolean alias.
- `pypdfbox/pdmodel/interactive/action/pd_page_additional_actions.py` + `pd_document_catalog_additional_actions.py`: 7 upstream-named aliases per Table 195/196 — `get/set_open_action`, `get/set_close_action` (page); `get/set_will_close`, `get/set_will_save`, `get/set_did_save`, `get/set_will_print`, `get/set_did_print` (catalog).
- `pypdfbox/pdmodel/font/pd_font_factory.py`: 5 parity additions — `create_font(font_dict, resource_cache=None)` extended for upstream signature parity (cache currently ignored), `create_simple_font(font_dict)` returns dispatched font only when PDSimpleFont, `create_cid_font(font_dict)` returns dispatched font only when PDCIDFont, `create_default_font(name)` builds Standard 14 PDType1Font with Helvetica fallback for unknown names + alias canonicalisation (`ArialMT` → `Helvetica`), `is_supported_subtype(subtype)` predicate over the known `/Subtype` set.
- `pypdfbox/pdmodel/interactive/action/pd_action_named.py` + `pd_action_java_script.py`: 4 `NAMED_ACTION_*` constants (NEXT_PAGE/PREV_PAGE/FIRST_PAGE/LAST_PAGE) + PDActionJavaScript `get_action()` upgraded to handle both `COSString` (returned via `.get_string()`) and `COSStream` forms (decodes body via `PDStream.create_input_stream` + UTF-8).
- `pypdfbox/pdmodel/pd_page_content_stream.py`: 13 path / clip / state operators — `curve_to_1` (`v`), `curve_to_2` (`y`), `clip_path` (`W`), `clip_path_even_odd` (`W*`), `fill_even_odd` (`f*`), `fill_and_stroke_even_odd` (`B*`), `close_fill_and_stroke` (`b`), `close_fill_and_stroke_even_odd` (`b*`), `set_dash_pattern(dash, phase)` (`[...] phase d`), `set_rendering_intent(intent)` (`/intent ri`), `set_flatness(flatness)` (`value i`), `concatenate_matrix(a..f)` alias for `transform`, `set_text_rendering_mode(mode)` (`<mode> Tr`) with 0-7 validation.

## Wave 27 — 3 parallel agents: 3 new typed XMP schemas

- `pypdfbox/xmpbox/xmp_media_management_schema.py` (NEW): `XMPMediaManagementSchema` (`xmpMM:` namespace `http://ns.adobe.com/xap/1.0/mm/`). 10 typed text-property accessors — `document_id`, `instance_id`, `original_document_id`, `version_id`, `rendition_class`, `rendition_params`, `manage_to`, `manage_ui`, `manager`, `manager_variant`. `DerivedFrom` / `Ingredients` deferred (need ResourceRef typed wrapper). Wired into `XMPMetadata.get/add_xmp_media_management_schema` + `DomXmpParser._SCHEMA_REGISTRY`.
- `pypdfbox/xmpbox/xmp_rights_management_schema.py` (NEW): `XMPRightsManagementSchema` (`xmpRights:` namespace `http://ns.adobe.com/xap/1.0/rights/`). Accessors — text `certificate` / `web_statement`, boolean `marked` (stored as upstream-style "True"/"False" text; reader accepts both capitalised and lowercase forms; None ≠ False), Bag-of-text `owners` (with `add_owner` helper), LangAlt `usage_terms(lang="x-default")`. Wired into XMPMetadata + parser registry.
- `pypdfbox/xmpbox/pdfa_extension_schema.py` (NEW): `PDFAExtensionSchema` (`pdfaExtension:` namespace + nested `pdfaSchema:` / `pdfaProperty:` / `pdfaType:` namespaces auto-registered on construction). Lite surface — `get_extension_schemas()` returns `list[dict[str, str]]` with `schema`/`namespaceURI`/`prefix` per declared extension; `add_extension_schema(schema, namespace_uri, prefix)` appends a Bag struct; `get_schemas_element()` raw passthrough; `get_count()`. Replaces the wave-24 placeholder in `XMPMetadata.get_pdfa_extension_schema` (was returning None). Full nested-struct typing for `pdfaSchema:property` + `pdfaSchema:valueType` deferred. Existing `test_xmp_metadata_parity::test_pdf_basic_schema_placeholders_return_none_when_absent` narrowed to AdobePDF-only since the PDF/A-extension placeholder is now real.

## Wave 28 — 20 parallel agents: XMP schemas, measurement / OCG-state / PDF 2.0 actions, fontbox tables, signature properties

- `pypdfbox/xmpbox/adobe_pdf_schema.py` (NEW): `AdobePDFSchema` (`pdf:` namespace `http://ns.adobe.com/pdf/1.3/`). Typed accessors for `Keywords`, `PDFVersion`, `Producer`. Replaces `XMPMetadata.get_adobe_pdf_schema` / `add_adobe_pdf_schema` / `create_and_add_adobe_pdf_schema` placeholders (with `get_pdf_schema` / `add_pdf_basic_schema` aliases). Registered in `DomXmpParser._SCHEMA_REGISTRY`. **Deviation**: the `setKeywordsProperty` / `setPDFVersionProperty` / `setProducerProperty` `TextType`-typed accessors are deferred until the `AbstractField` hierarchy lands; string-valued setters/getters provide full functional parity. Upstream `AdobePDFSchema` in PDFBox 3.0.x does not declare a `Trapped` property despite the Adobe `pdf:` namespace including it — mirroring upstream verbatim. The previously-deferred parity test asserting placeholder-returns-None was replaced with a real round-trip test.
- `pypdfbox/xmpbox/xmp_basic_job_ticket_schema.py` (NEW): `XMPBasicJobTicketSchema` (`xmpBJ:` namespace `http://ns.adobe.com/xap/1.0/bj/`) + co-located lite `JobType` (upstream is in `org.apache.xmpbox.type.JobType`). `JobRef` Bag entries are stored as plain dicts keyed by `id`/`name`/`url`; `get_jobs()` materialises typed `JobType` wrappers on demand. `JobType` is a lite value object (no `AbstractStructuredType` base) with `as_dict`/`from_dict` round-trip helpers. Registered with `XMPMetadata.add_xmp_basic_job_ticket_schema` / `create_and_add_basic_job_ticket_schema`.
- `pypdfbox/xmpbox/xmp_paged_text_schema.py` (NEW): `XMPageTextSchema` (`xmpTPg:` namespace `http://ns.adobe.com/xap/1.0/t/pg/`). Class name kept verbatim from upstream (`XMPageTextSchema`, not `XMPagedTextSchema`). `NPages` exposed as Python `int` via `get_n_pages` / `set_n_pages` (cluster #1 stores as string, coerces on read). `MaxPageSize` / `Colorants` / `Fonts` accept either struct-shaped dicts or plain strings — typed `Dimensions` / `Colorant` / `Font` struct wrappers deferred. Registered in `XMPMetadata` + parser registry.
- `pypdfbox/xmpbox/photoshop_schema.py` (NEW): `PhotoshopSchema` (`photoshop:` namespace `http://ns.adobe.com/photoshop/1.0/`) — 18 properties. Typed accessors return Python primitives (`str`, `int | None`, `list[str]`) rather than upstream `TextType` / `IntegerType` / `URIType` / `ProperNameType` / `DateType`; typed `*Property` getters/setters and `addProperty(AbstractField)` overloads deferred. `set_urgency` collapses upstream's `(String)` and `(Integer)` overloads into a single `int | str | None` setter. `TextLayers` (Seq<LayerType>) round-trip is deferred; constant mirrored, accessors not ported. Registered in `XMPMetadata` + parser registry.
- `pypdfbox/pdmodel/interactive/measurement/pd_measure_dictionary.py` + `pd_rectlinear_measure_dictionary.py` (NEW): typed `PDMeasureDictionary` (`/Type=Measure`) + `PDRectlinearMeasureDictionary` (`/Subtype=RL` with R/X/Y/D/A/T/S/O/CYX number-format-array accessors). Number-format arrays return `list[PDNumberFormatDictionary] | None` (Python has no `float[]`/array-type). `PDAnnotationPolygon.measure` / `PDAnnotationPolyline.measure` upgraded from raw `COSDictionary` to typed `PDMeasureDictionary` (replaces the wave-9 "deferred" placeholder; setters accept either form for back-compat).
- `pypdfbox/pdmodel/interactive/measurement/pd_number_format_dictionary.py` (NEW): typed `PDNumberFormatDictionary` (entries `U`/`C`/`F`/`D`/`FD`/`RT`/`RD`/`PS`/`SS`/`O`). Class constants `TYPE`, `LABEL_SUFFIX_TO_VALUE`, `LABEL_PREFIX_TO_VALUE`, `FRACTIONAL_DISPLAY_DECIMAL/FRACTION/ROUND/TRUNCATE`. `setFractionalDisplay` / `setLabelPositionToValue` raise Python `ValueError` instead of `IllegalArgumentException`.
- `pypdfbox/pdmodel/interactive/measurement/pd_viewport_dictionary.py` (NEW): typed `PDViewportDictionary` (`/Type=Viewport`, `BBox` typed `PDRectangle`, `Name`, `Measure` typed `PDMeasureDictionary`). 1:1 port — setters accept `None` to remove (consistent with sibling measurement classes).
- `pypdfbox/pdmodel/interactive/action/pd_action_set_ocg_state.py` (NEW): typed `PDActionSetOCGState` (`SUB_TYPE = "SetOCGState"`, `State` array of names + OCG refs, `PreserveRB`). No upstream PDFBox 3.0.x equivalent — added so factory yields a typed instance for `S = SetOCGState` instead of `PDActionUnknown`. Wired into `pd_action.py` factory dispatch.
- `pypdfbox/pdmodel/interactive/action/pd_action_go_to_dp.py` (NEW): typed `PDActionGoToDp` (`SUB_TYPE = "GoToDp"`) — PDF 2.0 / ISO 32000-2 §12.6.4.4 action with `/Dp` accessor. Class name spelled `GoToDp` (lowercase `p`) to match the spec sub-type. No upstream PDFBox 3.0.x or trunk equivalent.
- `pypdfbox/pdmodel/interactive/action/pd_action_rich_media_execute.py` (NEW): typed `PDActionRichMediaExecute` (`SUB_TYPE = "RichMediaExecute"`) — PDF 2.0 / ISO 32000-2 §13.6.4 action with `/TA`, `/TI`, `/CMD` accessors plus `/CMD/N` and `/CMD/A` convenience methods. No upstream PDFBox 3.0.x or trunk equivalent.
- `pypdfbox/text/pdf_text_stripper_by_area.py` (NEW): `PDFTextStripperByArea` extends `PDFTextStripper` with `add_region(name, rect)` / `remove_region` / `get_regions` / `get_text_for_region` / `extract_regions(page)`. Rectangle is in **PDF user space** (y-up, lower-left) — upstream uses Java AWT (y-down, top-left), the lite extractor stores positions in PDF user space already so we keep the rectangle in the same frame to avoid a per-call inversion that would surprise Python callers. Built on the lite `PDFTextStripper`, which does not yet reproduce upstream's reading order for composite-font / multi-column real-world PDFs; the ported `testSomeMethod` exercises the API surface against `eu-001.pdf` but defers exact-string assertions. `extract_regions` does the per-region binning directly via `_format_positions` rather than overriding `write_page`, since the lite stripper has no streaming `writePage` cycle.
- `pypdfbox/text/text_metrics.py` (NEW): `TextMetrics` data holder constructed from a `TextPosition`; ascent/descent seeded from font size with the upstream-documented 0.7 / -0.2 ratios; `get_x` / `get_y` / `get_height` / mutators for ascent/descent. **No direct upstream Java source** — shape conforms to upstream-documented contract.
- `pypdfbox/text/word_with_text_positions.py` (NEW): `WordWithTextPositions` data holder. Promoted from upstream's `private static final` inner class of `PDFTextStripper` (lines 2171-2191 of the 3.0 branch) to a top-level public class so external callers can consume the structure directly.
- `pypdfbox/fontbox/ttf/glyph_table.py` + `glyph_data.py` (NEW): `GlyphTable(TTFTable)` and `GlyphData` wrappers around fontTools' `glyf` table. `GlyphData.get_path()` returns a `fontTools.pens.recordingPen.RecordingPen` rather than a `java.awt.geom.GeneralPath` — callers replay `pen.value` onto any pen. `GlyphRenderer`, `GlyfSimpleDescript`, `GlyfCompositeDescript` are intentionally not ported — fontTools handles composite resolution and contour decoding. `BoundingBox` inlined into `glyph_data.py` (the wider `fontbox.util` package is not yet ported); will be moved when `fontbox.util` lands. `TTFTable.read` overridden to call `_bind(ttf)` since fontTools is itself the parser. `get_glyphs()` materialises `list[GlyphData]` rather than upstream's mutable array. Cache policy preserved (MAX_CACHE_SIZE=5000 / MAX_CACHED_GLYPHS=100). `TrueTypeFont.get_glyph_table()` + `get_glyph(gid)` lazy-cached convenience accessors added.
- `pypdfbox/fontbox/ttf/kerning_table.py` + `kerning_subtable.py` (NEW): `KerningTable` + `KerningSubtable` wrappers around fontTools' `kern` table. Public `get_kerning(left, right)` / `get_kerning(glyphs)`, `is_horizontal_kerning(cross)`, `get_subtables()`, `get_horizontal_kerning_subtable(cross)` semantics match upstream. Format-1 / format-2 / Apple AAT subtables are exposed but report 0 adjustment (same effective behaviour as upstream's "unsupported subtable" log). `TrueTypeFont.get_kerning_table()` lazy-cached accessor added (no direct upstream equivalent — upstream exposes via `getTableMap`).
- `pypdfbox/fontbox/ttf/vertical_header_table.py` + `vertical_metrics_table.py` (NEW): `VerticalHeaderTable` (vhea) + `VerticalMetricsTable` (vmtx) wrappers. Structural read-from-stream is preserved (so `read()`-direct unit tests still work), but when accessed via `TrueTypeFont.get_vertical_header()` / `get_vertical_metrics()` the typed snapshots are populated from `fontTools.ttLib`'s parsed `vhea` / `vmtx` rather than from the raw byte stream. Both return `None` when the font lacks the table.
- `pypdfbox/fontbox/ttf/glyph_substitution_table.py` (NEW): `GlyphSubstitutionTable(TTFTable)` wrapper around fontTools' `GSUB` table. **`get_gsub_data()` always returns `None`** — upstream's `GsubData` projection is built atop a bespoke `org.apache.fontbox.ttf.model.{GsubData, Language, ScriptFeature, MapBackedScriptFeature}` value-class graph not ported here; callers needing structured access use `get_raw_table()` to reach the fontTools GSUB instance. **`get_substitution()` applies single-substitution lookups only** (lookup type 1) — types 2-4 (multiple/alternate/ligature) and 5-8 (contextual/extension) are skipped (upstream supports type 7 wrap-and-redispatch; pypdfbox does not since fontTools inlines extension subtables). `selectScriptTag()` lacks `OpenTypeScript.INHERITED` / `TAG_DEFAULT` constants — approximated with literal `"DFLT"` check until `OpenTypeScript` is ported. Upstream `GlyphSubstitutionTableTest.testGetGsubData` is ported in spirit only — the original asserts `GsubData` shape against `Lohit-Bengali.ttf` + golden tables; pypdfbox port asserts `get_supported_script_tags` / `get_supported_feature_tags` against the existing `LiberationSans-Regular.ttf` fixture.
- `pypdfbox/fontbox/ttf/digital_signature_table.py` (NEW): `DigitalSignatureTable(TTFTable)` wrapper around fontTools' `DSIG` table. Exposes a richer accessor surface than upstream (`get_version`, `get_num_signatures`, `get_flag`, `get_signature_blocks`, `get_signature_block`, `populate_from_fonttools`) — upstream is a near-empty placeholder declaring only `TAG`. `TrueTypeFont.get_dsig()` lazy + cached, mirrors `get_header()` pattern.
- `pypdfbox/fontbox/cff/type2_char_string.py` (NEW): `Type2CharString` thin wrapper around `fontTools.misc.psCharStrings.T2CharString`. Constructor signature mirrors upstream (`font, font_name, glyph_name, gid, sequence, default_width_x, nominal_width_x`); accepts a fontTools `T2CharString`, raw bytecode, a program-token list, or `None`. Public surface: `get_gid` / `get_name` / `get_font_name` / `get_default_width_x` / `get_nominal_width_x` / `get_width` (via `T2WidthExtractor`) / `get_path` (via fontTools' BasePen) / `get_bounds` / `t2` property. All opcode interpretation, subroutine recursion, hint masks, flex/hflex/hflex1/flex1 expansion delegated to fontTools — the private `convertType1ToType2`, `convertType2Command`, `clearStack`, `expandStemHints`, `markPath`, `closeCharString2Path`, `addAlternatingLine/Curve`, `addCurve`, `addCommandList`, `split` helpers are intentionally not ported. `get_path()` returns draw-command tuples rather than `java.awt.geom.GeneralPath`; `get_width()` returns `float`. `CFFFont.get_type2_char_string(out_of_range_gid)` returns an empty wrapper rather than throwing — ergonomic deviation; callers detect via empty path.
- `pypdfbox/pdmodel/interactive/digitalsignature/pd_prop_build.py` + `pd_prop_build_data_dict.py` (NEW): typed `PDPropBuild` (`/Filter`, `/PubSec`, `/App` sub-dict accessors) + `PDPropBuildDataDict` (Name/Date/REx-Version/R-Revision/V-MinimumRevision/PreRelease/OS/NonEFontNoWarn/TrustedMode). Setters accept `None` to remove (Python-side convenience, behavior-compatible when given a value). `get_revision()` / `get_minimum_revision()` return Python `int` (unbounded, vs upstream `long`); missing-key default `-1`. `PDSignature.get_prop_build()` / `set_prop_build()` wired in.
- `pypdfbox/pdmodel/graphics/image/pd_image_x_object.py`: `is_stencil()` / `set_stencil(b)` upstream-`PDImage`-interface aliases added on top of existing `is_image_mask` / `set_image_mask`. `get_mask()` / `set_mask()` / `get_color_key_mask()` / `set_color_key_mask()` / `get_soft_mask()` / `set_soft_mask()` confirmed already present (prior wave) — `get_mask()` returns `None` for the COSArray (color-key) form; `get_color_key_mask()` returns `None` for the COSStream form. `getStencilMaskColorSpace` does not exist in upstream PDFBox 3.0.x — correctly skipped.
- `pypdfbox/pdmodel/graphics/optionalcontent/pd_optional_content_group_usage.py` (NEW): typed `PDOptionalContentGroupUsage` wrapper for the OCG `/Usage` sub-dict per PDF 32000-1 §8.11.4.4 Table 102 — no direct upstream class (Apache PDFBox returns the raw `COSDictionary`). Per-sub-dict wrappers `PDUsageCreatorInfo`, `PDUsageLanguage`, `PDUsageExport`, `PDUsageZoom`, `PDUsagePrint`, `PDUsageView`, `PDUsageUser`, `PDUsagePageElement` alias the underlying `COSDictionary` (no copying) so writes round-trip through the parent OCG dict; legacy `PDOptionalContentGroup.set_usage_view_state()` etc. and the new typed wrapper see the same state. State setters validate `ON` / `OFF` and normalise lowercase. `PDUsageZoom.min` / `max` surface `None` when missing rather than baking in PDF-spec defaults (0 / +∞). `PDUsageUser.name` handles both string and array-of-string forms (Table 102 allows either). `PDOptionalContentGroup.get_usage()` returns the new typed wrapper or `None`; `get_usage_dict()` exposes the raw upstream-shaped accessor; `get_or_create_usage()` materialises `/Usage` lazily for write-side ergonomics.

## Wave 29 — 20 parallel agents: structure attribute objects, PDF 2.0 catalog plumbing, fontbox cmap promotion, signature seed-values, tools, content-stream engine, inline images

- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_structure_class_map.py` (NEW): typed `PDStructureClassMap` extracted from the upstream-inline `getClassMap`/`setClassMap` block on `PDStructureTreeRoot`. `get_class_definitions()` returns `dict[str, list[PDAttributeObject]]`; `get_class(name)` returns `[]` for missing keys (uniform iteration); `add_class()` promotes a single-`COSDictionary` entry to a `COSArray` automatically when a second attribute is added under the same name. `PDStructureTreeRoot.get_class_map()` now returns `PDStructureClassMap | None` (was `dict[str, Any]`); setter accepts the typed wrapper, a `dict`, or `None`.
- `pypdfbox/pdmodel/documentinterchange/taggedpdf/pd_user_property.py` (NEW): typed `PDUserProperty` wrapper around `/N` / `/V` / `/F` / `/H`. Located in `taggedpdf` for proximity to `PDUserAttributeObject` (upstream sits in `logicalstructure`). `set_name` stores `/N` as `COSString` rather than upstream's `COSName` — ISO 32000-1 §14.8.5 specifies user-property name as a *text string*; reader accepts either form for back-compat. `__hash__` is identity-based since the underlying dictionary is mutable. `PDUserAttributeObject` gained typed `get_owner_user_properties()` / `set_user_properties` / `add_user_property` / `remove_user_property` / `user_property_changed`; the existing dict-form surface is preserved unchanged for back-compat.
- `pypdfbox/pdmodel/documentinterchange/markedcontent/pd_marked_content.py` + `pypdfbox/pdmodel/documentinterchange/taggedpdf/pd_artifact_marked_content.py` (NEW, fallback target): substitute for the requested `PDOwnerProperties` (verified absent in upstream — concept is just `/O` name in attribute objects, already covered by `PDStandardAttributeObject` / `PDUserAttributeObject`). `PDMarkedContent` accepts `Any` for `add_text(text)` / `add_x_object(xobject)` since the `text` cluster (TextPosition) and graphics-XObject cluster (PDXObject) have not landed yet. `PDArtifactMarkedContent.get_b_box` (camelCase→snake_case mapping `getBBox` → `get_b_b_ox` collapsed to `get_b_box`).
- `pypdfbox/pdmodel/pd_developer_extension.py` (NEW): typed `PDDeveloperExtension` wrapper for catalog `/Extensions` per PDF 32000-1 §7.12.2 / ISO 32000-2 §7.12.3. **No upstream Java class in PDFBox 3.0 or trunk** — only the COSName constants `BASE_VERSION` / `EXTENSION_LEVEL` / `EXTENSIONS` exist upstream. Wired into `PDDocumentCatalog.get_developer_extensions()` / `set_developer_extensions()` / `add_developer_extension()` / `remove_developer_extension()`. The `/URL` field is ISO 32000-2-only.
- `pypdfbox/pdmodel/documentinterchange/logicalstructure/pd_default_attribute_object.py` + `pd_parent_tree_value.py` (NEW, fallback target): substitute for the requested `PDDocumentRequirements` (verified absent in upstream — `/Requirements` is left as raw COSArray with no helper class upstream). `PDDefaultAttributeObject.get_attribute_value(name)` collapses upstream's public/protected `getAttributeValue(name)` and `getAttributeValue(name, default)` overloads into a single Python method with default kwarg. `PDParentTreeValue` validates the constructor argument as `COSArray | COSDictionary` and raises `TypeError` (upstream uses overloaded constructors for compile-time enforcement).
- `pypdfbox/pdmodel/page_layout.py` + `page_mode.py` (NEW): `PageLayout` and `PageMode` ported as `enum.StrEnum` (upstream uses Java typed enum). The `StrEnum` choice is the deliberate deviation that lets enum members compare equal to their underlying PDF name strings — required for back-compat with existing pre-typed callers/tests that pass and assert plain strings. `PDDocumentCatalog.get_page_layout()` / `get_page_mode()` return type widened from `str | None` to `PageLayout | None` / `PageMode | None`; setters accept the enum or the raw PDF name string. `from_string()` raises `ValueError` instead of `IllegalArgumentException`. Unrecognised dictionary names map to `None` (tolerant catalog read posture matching upstream).
- `pypdfbox/pdmodel/interactive/annotation/pd_appearance_content_stream.py` (NEW): `PDAppearanceContentStream` extends the lite `PDPageContentStream` rather than a separate `PDAbstractContentStream` base; constructor bypasses parent's PDPage/PDFormXObject branching because `PDAppearanceStream` does not yet inherit from `PDFormXObject` (deferred to rendering cluster). `PDAppearanceStream` gained `get_stream()` / `get_resources()` / `set_resources()` to support the writer; full `PDFormXObject` inheritance still deferred. Adds `set_stroking_color_on_demand`, `set_non_stroking_color_on_demand`, `set_stroking_color(components)`, `set_non_stroking_color(components)`, `set_border_line` (BS dashed/solid + array dash with PDFBOX-5266 fallback), `set_line_width_on_demand`, `draw_shape`, `get_appearance`. Located under `interactive/annotation` for cohesion with `PDAppearanceStream` (upstream sits at the `pdmodel` root).
- `pypdfbox/pdmodel/common/pd_destination_or_action.py` (NEW): concrete marker class with static `create(value)` dispatcher (Python has no interface-only construct). Arrays/names/strings → `PDDestination.create`; dicts → `PDAction.create`; `None` / other → `None`. `PDDestination` and `PDAction` do not subclass `PDDestinationOrAction` to keep their inheritance chains intact — use `is_destination_or_action(value)` for the upstream `instanceof` idiom. `PDDocumentCatalog.get_open_action` now delegates to this dispatcher (was inlining the dispatch); behaviour unchanged.
- `pypdfbox/pdmodel/interactive/digitalsignature/pd_seed_value_certificate.py` + `pd_seed_value_mdp.py` (NEW): typed `PDSeedValueCertificate` (`/Cert` sub-dict — `/Subject` array, `/SubjectDN`, `/KeyUsage` with `add_key_usage` + `add_key_usage_chars` (renamed from upstream's char-overload), `/Issuer`, `/OID`, `/URL`, `/URLType`, six `/Ff`-flag pairs, `FLAG_*` constants) + typed `PDSeedValueMDP` (`/MDP` — `get_p` / `set_p` range-validated 0..3 raising `ValueError`). `PDSeedValue.get_seed_value_certificate()` and `get_mdp()` return typed wrappers (was raw `COSDictionary`); setters accept either form. Added short aliases `get_certificate()` / `set_certificate()` on `PDSeedValue` (not present upstream); also `set_mpd()` mirroring upstream's typo'd `setMPD`.
- `pypdfbox/fontbox/cmap/cid_range.py` (NEW; promoted from private `_CIDRange`): public typed `CIDRange` class. Backwards-compat alias `_CIDRange = CIDRange` retained in `cmap.py`. `pypdfbox/fontbox/cmap/bf_char_entry.py` and `bf_char_range.py` (NEW, original): typed value objects for ToUnicode `bfchar` / `bfrange` triples — **no upstream counterparts** (upstream `CMapParser` inlines these into `CMap.addCharMapping`). `BFCharRange` supports both forms (single target with last-codepoint increment, or explicit per-code list); iterable, materialises into `BFCharEntry`s.
- `pypdfbox/pdmodel/font/pd_font_descriptor.py`: enrichment — added `set_font_bounding_box(PDRectangle)`, `get_average_width` / `set_average_width` upstream-named aliases for `get/set_avg_width`, `set_character_set` alias, `has_widths()` / `has_missing_width()` predicates, `get_cid_set` / `set_cid_set` (`/CIDSet` stream), `get_panose()` returning a new `PDPanose` 10-byte wrapper (`/Style/Panose`), `get_lang` / `set_lang` (PDF 32000-1 Table 122 — *not present in upstream PDFBox 3.0*, surfaced as a deliberate pypdfbox extension). Applied PDFBOX-429 `abs()` workaround to `get_cap_height` / `get_x_height` (was returning raw value, now matches upstream Java behaviour). New public class `PDPanose` (mirrors upstream `org.apache.pdfbox.pdmodel.font.PDPanose`) — minimal port: 10-byte payload, `get_bytes`, `get_family_class`, `get_serif_style`. Per-byte category accessors deferred to font-subsetting cluster.
- `pypdfbox/pdmodel/pd_viewer_preferences.py`: no changes required — existing implementation already exceeds upstream PDFBox 3.0 surface (upstream has no `PickTrayByPDFSize` / `NumCopies` / `PrintPageRange` / `Enforce` accessors; pypdfbox already exposes them as PDF 32000-1 §12.2 / 32000-2 enrichment).
- `pypdfbox/pdmodel/graphics/color/pd_output_intent.py`: enrichment — added `(document, color_profile)` constructor mirroring upstream `PDOutputIntent(PDDocument, InputStream)` (auto-derives `/N` from ICC colour-space signature bytes 16–19 per ICC.1:2010 §7.2 Table 18 with optional `num_components` override; embeds the ICC blob as flate-encoded `PDStream` under `/DestOutputProfile`). Added `get_dest_output_intent()` upstream-named alias returning the raw `COSStream`. Added PDF 2.0 / ISO 32000-2 §14.11.5 typed accessors `get_dest_output_profile_ref()` / `set_dest_output_profile_ref()` (not in upstream 3.0). Internal `_configure_output_profile()` mirrors upstream private helper; `_icc_num_components()` parses the ICC colour-space signature.
- `pypdfbox/xmpbox/exif_schema.py` (NEW, lite): `ExifSchema` (`exif:` namespace `http://ns.adobe.com/exif/1.0/`). Simple-typed properties only. Rational-typed properties (CompressedBitsPerPixel, ExposureTime, FNumber, ShutterSpeedValue, ApertureValue, BrightnessValue, ExposureBiasValue, MaxApertureValue, SubjectDistance, FlashEnergy, FocalLength, FocalPlaneXResolution, FocalPlaneYResolution, ExposureIndex, DigitalZoomRatio, GPSAltitude, GPSDOP, GPSSpeed, GPSTrack, GPSImgDirection, GPSDestBearing, GPSDestDistance) — constants exposed; accessors omitted until `RationalType` lands. GPSCoordinate properties (GPSLatitude, GPSLongitude, GPSDestLatitude, GPSDestLongitude) — constants only. Typed-struct properties (OECF, SpatialFrequencyResponse, CFAPattern, Flash, DeviceSettingDescription) — constants only. Wired into `XMPMetadata` + parser registry.
- `pypdfbox/xmpbox/tiff_schema.py` (NEW, substitute target): `TiffSchema` (`tiff:` namespace `http://ns.adobe.com/tiff/1.0/`). Substitute for the requested `CameraRawSchema` — verified absent in PDFBox 3.0.x's xmpbox. Simple-typed properties; Rational properties (XResolution / YResolution / WhitePoint / PrimaryChromaticities / YCbCrCoefficients / ReferenceBlackWhite) stored as upstream's `"num/den"` text form pending a `RationalType` typed-struct port. Wired into `XMPMetadata` + parser registry.
- `pypdfbox/tools/extracttext.py` (NEW): port of upstream `org.apache.pdfbox.tools.ExtractText` (PicoCLI → argparse). Surfaces `-i/--input`, `-o/--output`, `-password`, `-encoding`, `-startPage`, `-endPage`, `-sort`, `-console`, `-addFileName`, `-append`. Permission gate via `get_current_access_permission().can_extract_content()`. Omitted upstream flags: `-html`, `-md`, `-rotationMagic`, `-debug`, `-alwaysNext`, `-ignoreBeads`, embedded-PDF extraction (needs `PDDocumentNameDictionary` enumerable).
- `pypdfbox/tools/encrypt.py` (NEW): port of upstream `org.apache.pdfbox.tools.Encrypt`. Default `-keyLength=256`. PicoCLI's "boolean defaulting to true" `-canX` flags surfaced as paired `-canX` / `--no-canX` argparse switches (semantics unchanged). `-canPrintFaithful` maps to `AccessPermission.set_can_print_degraded` (the spec bit underpinning faithful/high-quality printing — same as upstream). Cert loading uses `cryptography.x509.load_der_x509_certificate` with PEM fallback (Java `CertificateFactory.generateCertificate` sniffs both). Re-encrypting an already-encrypted file emits the upstream "Document is already encrypted" message and exits 0.
- `pypdfbox/contentstream/pdf_graphics_stream_engine.py` (NEW): abstract `PDFGraphicsStreamEngine` subclass of `PDFStreamEngine` registering all standard graphics/path/clip/paint/state/colour/text/marked-content/image operator processors and forwarding path-construction (`m`, `l`, `c`, `v`, `y`, `re`, `h`), painting (`S`, `s`, `f`, `F`, `f*`, `B`, `B*`, `b`, `b*`, `n`), clip (`W`, `W*`), shading (`sh`), and graphics-state save/restore (`q`, `Q`) to abstract hook methods (`move_to`, `line_to`, `curve_to`, `append_rectangle`, `close_path`, `stroke_path`, `fill_path`, `fill_and_stroke_path`, `end_path`, `clip`, `shading_fill`, `draw_image`, `get_current_point`). `WIND_NON_ZERO` / `WIND_EVEN_ODD` exposed as plain int constants (no `java.awt.geom.Path2D` dependency). Added `get_page()` and an overridable `transformed_point(x, y)` helper. `PDFRenderer` left untouched — it overrides `process_operator` entirely with its own dispatch table; rewiring would force it to provide stubs for hooks that never fire. `PDFGraphicsStreamEngine` is in place for future use by tools/extractors and a future renderer rewrite.
- `pypdfbox/pdmodel/graphics/pd_property_list.py`: enrichment — fixed `create()` to return a bare `PDPropertyList(dictionary)` for unknown / missing `/Type` (was returning `None`), matching upstream's `else` branch. Added re-export module `pypdfbox/pdmodel/documentinterchange/markedcontent/pd_property_list.py` aliasing the implementation at the upstream-canonical Java package path. `get_metadata()` / `set_metadata()` accessors **deliberately not added** — upstream `org.apache.pdfbox.pdmodel.documentinterchange.markedcontent.PDPropertyList` (3.0 branch) has no such methods; CLAUDE.md rule "Do not invent abstractions" overrides the task wording.
- `pypdfbox/pdmodel/graphics/image/pd_inline_image.py` (NEW): port of upstream `PDInlineImage`. Constructor takes `(parameters, data, resources)` with eager filter-chain decode through `FilterFactory.INSTANCE.get_filter(...)` and `parameters.add_all(decode_result.parameters)` repair step. Two-key (short/long) lookups for `/W /Width`, `/H /Height`, `/BPC /BitsPerComponent`, `/CS /ColorSpace`, `/F /Filter`, `/D /Decode`, `/IM /ImageMask`, `/I /Interpolate`. `get_color_space()` with short-name expansion (`/G→/DeviceGray`, `/RGB→/DeviceRGB`, `/CMYK→/DeviceCMYK`) + `/I` and `/Indexed` array rebuild; stencil fallback to `PDDeviceGray.INSTANCE`; raises `OSError` (project Java `IOException` mapping) when missing on non-stencil. `get_image()` / `get_stencil_image()` / `get_raw_raster()` / `get_raw_image()` not ported — replaced with `to_pil_image()` (DCT/JPX + raw 8-bit DeviceGray/DeviceRGB only); full sampled-image decoding lands with the rendering cluster. **BI/ID/EI operator stubs left as no-ops** — full engine integration of inline-image construction (the `show_inline_image` hook on the stream engine) deferred to the rendering-cluster wave.
